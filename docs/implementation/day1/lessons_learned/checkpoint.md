# Lessons Learned — checkpoint (Part B)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| checkpoint-b02 | L (per frozen EXECUTION_DAG.md) | L — estimate held; the porcelain v2 grammar (5 distinct record shapes: changed, renamed/copied with NUL-separated orig-path, unmerged with 3 conflict stages, untracked, ignored, plus `#` branch headers) is genuinely dense, and getting the resolver to correctly distinguish main vs. linked worktrees needed a real `git worktree add` integration test, not just unit fixtures | 5 (per DAG) | 7 (4 implementation + 3 test files, vs. DAG's undifferentiated count of 5 — the DAG estimate likely counted implementation files only and didn't separately budget for test-file count, since `gitx_test.go` as shared scaffolding, `resolver_test.go`, and `porcelain_test.go` ended up as three separate files by design choice, not scope creep) | n/a — DAG has no duration field | ~1 continuous execution pass across two sessions (first session was interrupted by a rate limit after implementation files were written but before tests/docs/commit; resumed from on-disk state per lead's verification, no rework needed) | None beyond the documented one (contract-integrator-07, already satisfied) | None — all files landed inside the pre-declared `internal/gitx/**` exclusive path; no path required that agents/checkpoint.md didn't already grant | 1) Session-level: work was interrupted by a rate limit mid-task with zero commits made, which meant the lead had to independently re-verify on-disk state (gofmt/build/vet/test) before resuming — the interruption cost a round-trip of lead verification but no implementation rework, since the four implementation files were already correct and complete. 2) DAG-level: none — dependency (contract-integrator-07) was genuinely satisfied and the node was correctly unblocked. | The interrupted-session recovery pattern worked because artifacts were already durable on disk (real files, not just conversational state) even though no git commit existed yet — this is a small but real instance of the same "durable evidence over conversational claim" principle the Constitution enforces for Progress Tree nodes (§6). A commit is not the only durable-evidence boundary; uncommitted-but-on-disk work should still be independently verifiable, and it was. | 1) The DAG's "Est. Files" column conflates implementation and test files into one number; splitting it into two columns (or documenting the convention explicitly) would make later actual-vs-estimate comparisons less noisy — this row's "7 vs 5" delta is almost entirely a counting-convention mismatch, not scope growth. 2) `ParsePorcelainV2`'s fail-closed-on-unknown-record-type behavior (reject rather than skip) was a design call not spelled out in agents/checkpoint.md Part B's deliverable list; worth stating explicitly in future packets for any parser sitting on an integrity boundary, so it's a specified requirement rather than an implementer's inference from Constitution §6 by analogy. 3) Confirm mid-task interruption recovery (rate limit, crash, etc.) is itself something Preflight's own Progress Tree/State Checkpoint model should be exercised against during its own dogfooding pass — this session was a small real-world instance of exactly the crash-recovery scenario checkpoint-a01's required tests will need to cover formally. |
| checkpoint-b03 | M (per frozen EXECUTION_DAG.md) | M — estimate held; the digest design (what goes in, what stays out, how to encode it collision-safely) was the real work, not the plumbing. One genuine design decision surfaced that no governing doc pre-answered: whether upstream ahead/behind counts belong in the digest (decided: no — they move on `git fetch`, which must not invalidate an FR-149 resume; recorded as a documented assumption in the progress artifact rather than a contract question, since it's inside this role's owned surface) | 3 (per DAG) | 3 (fingerprint.go, numstat.go, fingerprint_test.go — matches the DAG estimate exactly this time, consistent with b02's lesson that the DAG counts total files; all numstat tests were deliberately named TestFingerprint* so the DAG's frozen `-run Fingerprint` validation command exercises them) | n/a — DAG has no duration field | ~1 continuous execution pass, single session, no interruption (~20 min wall clock) | None — checkpoint-b02's status/resolver output plus two new pinned `git diff --numstat -z` invocations covered every input the fingerprint needs; the porcelain `--branch` headers already carried HEAD OID/branch identity, so no extra `git rev-parse HEAD` round-trip was needed | None — all files landed inside `internal/gitx/**` as pre-declared; no new path | None. One behavior was empirically verified before implementation rather than assumed: `git diff --cached --numstat` on an unborn branch diffs against the empty tree (probed in a scratch repo, then locked in by TestFingerprintUnbornBranch) | None material — the pre-implementation scratch-repo probe of numstat `-z` record shapes (rename = counts-with-trailing-tab + two NUL-separated path tokens) cost one small command round-trip and prevented a whole write-test-fail-fix cycle; cheap probes before parser-writing are net token-positive | 1) For any hash/digest contract, the packet should state up front which fields are identity-bearing vs informational — the ahead/behind exclusion was the only judgment call in this node, and a one-line packet note ('remote-tracking divergence is/is not part of snapshot identity') would have removed it. 2) Versioning the canonical encoding from day one (FingerprintSchema as the first hashed field) makes future field additions a clean v2 bump instead of a silent incompatibility; recommend the same pattern for every digest the checkpoint role produces (state-checkpoint manifest, repo-checkpoint verification). 3) b02's suggestion to add a live-repo unmerged-conflict test 'if the fingerprint branches on unmerged state' was evaluated: the fingerprint does not branch on unmerged state (conflict entries flow through the same canonical encoding as any other Entry), so the deferral stands and the note now points at checkpoint-b04, which does branch on recoverability. |
| checkpoint-a01 | M (per frozen EXECUTION_DAG.md) | S/M — the SQL itself is a verbatim §12.2 transcription (foundation-06's precedent made every judgment call — numbering at the range lower bound, no FK to not-yet-existing tables, index-with-owning-table convention — already documented, so nothing had to be rediscovered); the only real work was constraint-behavior tests and the cross-role test conflict below | 6 (per DAG) | 6 (3 .sql + 1 new test file + progress artifact + lessons row — matches the DAG count under the total-files convention established in the b02 row) | n/a — DAG has no duration field | ~1 continuous pass, single session (~25 min wall clock including the main-merge pre-step) | REAL CROSS-ROLE CONFLICT: foundation's migrate_test.go asserts the exact embedded migration set (len==4, CurrentVersion==4), so adding ANY file to the shared migrations/ directory breaks 3 foundation tests — a structural collision the DAG did not anticipate, and one that will hit predictor-01 (0040) and every later range identically. Handled per Constitution §4.4: documented change request in the progress artifact, no foundation file touched, failures left visible for integration. | migrations_checkpoint_a_test.go had to be created inside foundation's internal/storage/sqlite directory (not in checkpoint's declared paths) because the DAG's frozen validation command targets that package — flagged as an ownership-map gap for contract-integrator | Foundation's 3 exact-count tests fail on this branch with my files present (see unexpected_dependencies); my own validation command passes | None — reading foundation-06's progress-artifact notes BEFORE writing any SQL (its 0000-vs-0001 numbering bug, the active_node_id no-FK precedent) prevented at least two would-be write-test-fail cycles; reading the prior role artifact was strongly net token-positive | 1) Any repo-wide test that asserts an exact inventory of a SHARED, append-only resource (embedded migration set, schema_migrations max version) is a time bomb for every later contributor to that resource; assert your own subset, not the universe. Preflight's own qa role should lint for this pattern. 2) The DAG should state explicitly, for each `-run MigrationNNNN` validation command, which package the test file is expected to live in and who owns it — the current ownership map technically makes the frozen validation command unsatisfiable without stepping into foundation's directory. 3) Migration-range interleaving hazard for Preflight itself: migrate.go skips versions <= CurrentVersion, so a dev DB migrated to 0030 before claude-provider lands 0010_turns will NEVER apply 0010; fine for fresh test DBs, but worth a foundation-level guard (e.g. detect gaps below CurrentVersion) before any long-lived dev database exists. |
| checkpoint-b01 | S (per frozen EXECUTION_DAG.md) | S — estimate held; a single verbatim §12.2 table plus constraint tests. The one genuine decision (turn_id FK to a not-yet-existing turns table) had an exact foundation precedent (tasks.active_node_id, 0004_tasks.sql header) to follow, so it cost a documented assumption rather than design work | 3 (per DAG) | 4 (1 .sql + 1 test file + progress artifact + lessons row — one over the DAG count; the DAG's S-node file estimates appear not to budget the per-node progress/lessons doc updates the execution plan itself mandates) | n/a — DAG has no duration field | ~15 min wall clock, same session as checkpoint-a01 | Same single cross-role conflict as checkpoint-a01 (foundation's exact-count migration tests) — no NEW dependency; one root cause, filed once. Also confirmed §12.2's declared turn_id FK is unimplementable until claude-provider's 0010-0019 range lands: cross-range FK dependencies mean the canonical schema is not cleanly partitionable by the frozen migration-range ownership, and each such edge needs a documented deviation + follow-up migration | migrations_checkpoint_b_test.go in foundation's directory, same rationale and flag as checkpoint-a01's row | Foundation's 3 exact-count tests (shared root cause with a01, pre-documented) | None — a01 had already paid every discovery cost (helpers, seed chain, test-file-location decision); b01 reused all of it. Sequencing the two sibling migration nodes in one session was cheaper than the DAG's assumption they could run fully independently | 1) The frozen migration-range table (CONTRACT_FREEZE.md) partitions TABLES cleanly but not FK EDGES: repository_checkpoints->turns and file_changes->turns cross range boundaries in the wrong dependency direction (lower range lands later). Contract-integrator may want a one-line policy in CONTRACT_FREEZE.md: cross-range FKs are always deferred to a follow-up migration by the FK-holding role once the target range lands, per the 0004 active_node_id precedent — otherwise every affected role re-derives this individually. 2) Migrate()'s skip-below-current semantics plus out-of-order range landing means any long-lived dev DB created this week will silently never get claude-provider's 0010-0019; flagged in a01's row, repeated here because b01's turn_id is exactly the column that will dangle. |
