# Lessons Learned — checkpoint (Part B)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| checkpoint-b02 | L (per frozen EXECUTION_DAG.md) | L — estimate held; the porcelain v2 grammar (5 distinct record shapes: changed, renamed/copied with NUL-separated orig-path, unmerged with 3 conflict stages, untracked, ignored, plus `#` branch headers) is genuinely dense, and getting the resolver to correctly distinguish main vs. linked worktrees needed a real `git worktree add` integration test, not just unit fixtures | 5 (per DAG) | 7 (4 implementation + 3 test files, vs. DAG's undifferentiated count of 5 — the DAG estimate likely counted implementation files only and didn't separately budget for test-file count, since `gitx_test.go` as shared scaffolding, `resolver_test.go`, and `porcelain_test.go` ended up as three separate files by design choice, not scope creep) | n/a — DAG has no duration field | ~1 continuous execution pass across two sessions (first session was interrupted by a rate limit after implementation files were written but before tests/docs/commit; resumed from on-disk state per lead's verification, no rework needed) | None beyond the documented one (contract-integrator-07, already satisfied) | None — all files landed inside the pre-declared `internal/gitx/**` exclusive path; no path required that agents/checkpoint.md didn't already grant | 1) Session-level: work was interrupted by a rate limit mid-task with zero commits made, which meant the lead had to independently re-verify on-disk state (gofmt/build/vet/test) before resuming — the interruption cost a round-trip of lead verification but no implementation rework, since the four implementation files were already correct and complete. 2) DAG-level: none — dependency (contract-integrator-07) was genuinely satisfied and the node was correctly unblocked. | The interrupted-session recovery pattern worked because artifacts were already durable on disk (real files, not just conversational state) even though no git commit existed yet — this is a small but real instance of the same "durable evidence over conversational claim" principle the Constitution enforces for Progress Tree nodes (§6). A commit is not the only durable-evidence boundary; uncommitted-but-on-disk work should still be independently verifiable, and it was. | 1) The DAG's "Est. Files" column conflates implementation and test files into one number; splitting it into two columns (or documenting the convention explicitly) would make later actual-vs-estimate comparisons less noisy — this row's "7 vs 5" delta is almost entirely a counting-convention mismatch, not scope growth. 2) `ParsePorcelainV2`'s fail-closed-on-unknown-record-type behavior (reject rather than skip) was a design call not spelled out in agents/checkpoint.md Part B's deliverable list; worth stating explicitly in future packets for any parser sitting on an integrity boundary, so it's a specified requirement rather than an implementer's inference from Constitution §6 by analogy. 3) Confirm mid-task interruption recovery (rate limit, crash, etc.) is itself something Preflight's own Progress Tree/State Checkpoint model should be exercised against during its own dogfooding pass — this session was a small real-world instance of exactly the crash-recovery scenario checkpoint-a01's required tests will need to cover formally. |
