# Lessons Learned — claude-provider (Wave: claude-provider-01/02/03)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|

## Wave 2

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-04 | L (~6 files per DAG estimate) | M-L — matched file count, but overall effort landed closer to M than the DAG's L once the design question (struct-to-event cardinality) was resolved; the bulk of the work was designing the mapping, not writing the JSON-marshaling code itself | 6 | 3 (normalizer.go, normalizer_test.go, privacy_test.go — the DAG's 6-file estimate likely anticipated a separate file per source-struct normalizer; this role consolidated all four into one normalizer.go since they share the envelope/digestKey helpers and splitting them would have meant either duplicating those helpers or adding a fourth internal-only file just to hold them) | not tracked (no wall-clock instrumentation available in this environment) | one continuous pass, no interruption | Found and reused `domain.IDGenerator`/`domain.Clock` (internal/domain/clock.go) as the injection seam for EventID/timestamp generation instead of depending on foundation's not-yet-built internal/idgen (foundation-06, which this node does not depend on) — this was not called out explicitly in the packet but is consistent with CONTRACT_FREEZE.md's "Bootstrap did NOT freeze... owning roles MAY find they need additional fields" spirit applied to *dependencies* rather than fields | None beyond the DAG's own listed artifacts (normalizer.go, normalizer_test.go, privacy_test.go) | None — contract-integrator-04's pkg/protocol/v1.EventType taxonomy already contained every event type needed (EventProviderContextObserved/UsageObserved/QuotaObserved/TurnStarted/TurnCompleted/TurnFailed/RateLimitHit); no gap to report | None observed within this node's execution | 1) The DAG's "High risk — sole path from raw provider payloads into the frozen event envelope" note was accurate in spirit but the actual risk materialized as a *design* question (how many events does one StatusLineSnapshot become?) rather than a missing-contract-type question — worth noting in future DAG risk columns that normalization nodes carry cardinality-mapping risk, not just taxonomy-completeness risk. 2) Reusing Wave-1 parsers' own fixtures (via Parse* calls inside this node's tests) rather than adding a fourth telemetry-specific fixture directory kept the artifact count down and meant a change to a Wave-1 fixture automatically re-exercises this node's normalization logic too — recommend other normalization-layer nodes in future waves do the same rather than duplicating fixtures one layer up |
| claude-provider-06 | S | S — matched estimate; the stub nature (per the DAG's own note that runtime-b01's CLI doesn't exist yet) kept the mechanical work small, but reading Preflight_ADD.md Appendix E.2/E.3 closely enough to spot the PascalCase-vs-kebab-case CLI-subcommand discrepancy against agents/runtime.md/the DAG took real attention despite the low file count | 3 | 3 (plugin.json, hooks.json, README.md) | not tracked | one continuous pass | None | README.md was not explicitly named in the DAG's artifact list but was necessary to document the forward-looking-stub status and the ADD-vs-runtime.md CLI-casing conflict rather than silently picking one | Discovered a genuine cross-document discrepancy: Preflight_ADD.md Appendix E.3 spells CLI subcommands PascalCase (`preflight hook claude UserPromptSubmit`) while agents/runtime.md's P0 command list and the DAG's own validation command for this exact node spell them kebab-case (`preflight hook claude user-prompt-submit`). Per task instructions this did not rise to a full node-blocking STOP (a defensible default existed: follow this node's own frozen validation command), but it is recorded in both this progress artifact and integrations/claude/README.md rather than silently resolved, since only contract-integrator can actually reconcile it | None observed | 1) Even an "S / stub acceptable" node benefited from cross-checking against the ADD section this role explicitly owns (Appendix E.2/E.3) rather than only the DAG/packet — the packet's one-line mention ("Claude plugin/hooks example invoking `preflight hook claude ...`") under-specified the casing question entirely, and the ADD had the fuller (if now-conflicting) answer. 2) Worth a contract-integrator pass to either fix Appendix E.3 to kebab-case or fix agents/runtime.md's P0 list to PascalCase — right now two frozen-ish documents disagree on a detail a future role (runtime-b01) will have to pick one of when it actually builds the CLI |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-01 | M | M — matched estimate; the pointer-heavy struct shape and nested-optional decoding took more care than the LOC count suggests, but no new abstractions were needed | 8 | 7 (statusline.go, statusline_test.go, + 5 fixture JSON files — all 5 fixtures were pre-existing/untracked from an interrupted prior attempt, inspected and kept rather than authored fresh) | not tracked (no wall-clock instrumentation available in this environment) | one continuous pass, no interruption | None | None beyond the DAG's own listed artifacts | Pre-existing fixtures needed inspection before trusting them — they turned out to align exactly with ADD §22.5's field list, so no rework was needed, but that had to be verified rather than assumed | None observed within this node's execution | The DAG's "Needs representative fixtures" risk note under claude-provider-01 was accurate — worth explicitly flagging in the DAG (not just the risk column) that a role should always inspect leftover/pre-existing fixtures for schema alignment before reusing them, since a stale attempt could just as easily have been wrong |
| claude-provider-02 | M | M — matched estimate. The privacy-hashing requirement (SHA-256 immediately on parse, no raw-text field anywhere) was straightforward once the EvaluateTurnRequest.PromptHash naming convention in internal/app/ports.go was found and mirrored | 6 | 9 (userpromptsubmit.go, userpromptsubmit_test.go, + 5 request fixtures + 2 golden response fixtures) | not tracked | one continuous pass | None | Golden response files (response_allow.golden.json, response_block.golden.json) were not explicitly named in the DAG's artifact list but were necessary to satisfy the packet's "block/allow response golden files" Tests requirement | The ADD shows only the block-response JSON shape (§22.3), not an explicit allow-response shape — had to make and document a judgment call (bare `{}`, omitting the decision key) rather than have a frozen shape to implement against | None observed within this node's execution | Consider freezing the allow-response shape for UserPromptSubmit explicitly in a future contract-integrator pass, since claude-provider-06 (CLI wrapper, later wave) and any other provider adding a similar hook will otherwise each make their own judgment call independently |
| claude-provider-03 | M | M — matched estimate on files, slightly more iteration than 01/02 because the first version of the classifyFailure heuristic and its accompanying test fixture (unknown_category.json) disagreed with each other on a status-code edge case (599) and had to be reconciled | 6 | 12 (stop.go, stop_test.go, + 4 Stop fixtures + 7 StopFailure fixtures — StopFailure needed more fixtures than Stop to cover each domain.FailureClass branch the packet asks for) | not tracked | one continuous pass, plus one short follow-up pass (after a mid-session interruption) to add the missing stop_test.go and fix the fixture/classifier mismatch | None | None beyond the DAG's own listed artifacts | 1) A session interruption (harness-level rate limit, not a code issue) landed stop.go without its test file; the coordinator caught this via independent verification before it was reported complete — Progress Tree discipline (don't trust "done" without evidence) worked as intended here, just enforced externally instead of by this role's own review pass. 2) The first draft of unknown_category.json included status_code: 599, which classifyFailure's own 5xx-range fallback rule correctly mapped to FailureProviderInternal rather than FailureUnknown — the fixture, not the classifier, was wrong for what it claimed to test, and was corrected by removing the status code so the fixture genuinely exercises the unmapped-error-type path | Minor: one wasted test-run cycle discovering the fixture/classifier mismatch described above — would have been avoided by writing the classifier's decision table as a comment first and deriving fixtures from it, rather than writing fixtures and classifier in parallel from intuition | 1) When a task involves both a classification heuristic and hand-built fixtures meant to exercise its branches, write the branch table first as a single source of truth and derive both the code's switch/case and the fixtures from it — reduces the chance of the fixture and the implementation independently "agreeing to disagree." 2) A harness-level interruption mid-node is indistinguishable, from a coordinator's view, from a role silently stopping early — the coordinator's independent gofmt/build/vet/test verification (rather than trusting the role's self-report) is the correct mitigation and should stay standard practice, not just a fallback for this incident |

## Wave 4

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-05 | M (per DAG: 300 LOC est., 6 files est.) | M — matched the DAG's complexity band; the schema-design judgment call (which table(s) to persist into, given ADD §12.2 lists four plausible candidates — events/turns/turn_usage/quota_observations/context_observations — and this role's own normalizer only produces generic v1.Event values, not turn/usage-shaped structs yet) took more thought than the write path itself, which was mechanical once the target table was fixed | 6 | 3 (migrations/0010_events.sql, store.go, store_test.go — fewer than estimated because the generic `events` table absorbs all of this role's normalized event types uniformly; a turns/turn_usage/quota_observations/context_observations-per-table design would have needed one migration + one store file per table instead of one of each) | 300 (DAG estimate) | not tracked (no wall-clock instrumentation available in this environment); one continuous pass, no interruption | **The task's own step 1 (merge main before starting) surfaced a real, load-bearing dependency**: this node cannot compile or be tested at all without foundation-06's `internal/storage/sqlite` package (DB.WithTx, QuerierFromContext, Migrate, AllMigrations) and its 0001-0004 migrations being present on this branch — none of that existed on `day1/claude-provider` before the Wave-4 merge (this branch's Wave 1/2 work, claude-provider-04, was deliberately built to depend only on `internal/domain`'s Clock/IDGenerator ports, not on any concrete storage engine, exactly so it would not block on foundation's timeline — see the Wave 2 lessons entry for claude-provider-04). The merge itself was a clean fast-forward (branch HEAD was already an ancestor of origin/main) with zero conflicts, so the "unexpected dependency" here was fully anticipated by the task brief, not a surprise found mid-implementation | None beyond the three listed artifacts | **A real, if narrow, SQLite syntax pitfall**: `INSERT ... ON CONFLICT(col) DO NOTHING` against a *partial* unique index (`CREATE UNIQUE INDEX ... WHERE idempotency_key IS NOT NULL`, which is ADD §12.3's own literal required-index SQL) fails at runtime ("ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint") unless the INSERT's own ON CONFLICT clause repeats the exact same WHERE predicate. This was not discoverable by reading the schema or the Go driver docs — it only surfaced when the first idempotency test actually ran against a real (temp-file) SQLite database. Cost: one full test-run/fix/retest cycle, a few minutes, not a structural blocker. **Second, more consequential finding**: after adding migrations/0010_events.sql, three pre-existing tests in internal/storage/sqlite/migrate_test.go (TestAllMigrations_LoadsCoreSchemaFiles, TestCoreMigrations_FromEmptyDatabase, TestCoreMigrations_ReopenFromFile_AppliesOnce) started failing because they hardcode "exactly 4 migrations exist" / "CurrentVersion == 4". This is a file outside this role's exclusive paths (owned by foundation, not claude-provider), so it was not fixed here — documented as a blocker in the progress artifact instead, per Constitution §4 rule 4. This is a *structural* gap in foundation's own test isolation (any second role to land a migration range would trip the same hardcoded-count assertions), not something specific to this node's schema choices, and is worth flagging loudly since checkpoint/predictor/runtime will each hit the identical failure the moment they land their own first migration, unless foundation/contract-integrator fixes migrate_test.go's assertions to be range-agnostic (e.g. "contains at least these named tables" rather than "len(migrations) == N") before then | Minor: the ON CONFLICT/partial-index syntax issue above cost one avoidable retest cycle; now documented in store.go's own doc comment so no future reader of this codebase (in any role) has to rediscover it independently | 1) **Flag for contract-integrator/foundation**: internal/storage/sqlite/migrate_test.go's hardcoded migration counts (`len(migrations) == 4`, `CurrentVersion == 4`) will break for every subsequent role that adds its own migration range (checkpoint 0020-0039, predictor 0040-0049, runtime 0050-0059) — recommend changing those assertions to check for named tables' presence rather than an exact count/version, before more roles hit the same wall independently. 2) The DAG's per-node LOC/file-count estimates for storage-writing nodes (this one: 300 LOC / 6 files) appear to assume one file pair (migration + store) per logical table; when a role's actual data model collapses cleanly into one shared table (as this role's four event subtypes do, via the generic `events` log), the estimate should be read as an upper bound, not a target — forcing artificial per-subtype file splits to "match the estimate" would have added complexity with no corresponding benefit here. 3) Worth adding a repo-wide `go test ./...` (not just the node's own scoped validation command) as a standard last step for any node that adds a migration file, specifically because of the cross-role migrate_test.go fallout described above — the node's own scoped validation command (`go test ./internal/telemetry/claude/... -run Idempotent`) would not have surfaced this by itself. |

## Wave 5

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-07 | L (per DAG: 300 points, ~10h) | M-L — the DAG's own risk note ("Medium — raw-prompt-absence assertion is a hard privacy gate") was the accurate cost driver: writing the table-driven fixture-pipeline test itself was mechanical once the existing normalizer/store surface was understood, but designing a privacy-gate test worth trusting (one that is actually falsifiable, not just a test that happens to pass) took real deliberation | 2 (per DAG's own 2-file-ish estimate, though the DAG doesn't state a file count explicitly for this node) | 2 (internal/telemetry/claude/fixture_suite_test.go, testdata/provider-events/claude/statusline/compacted.json) — deliberately did NOT add a new duplicate-event fixture file (reused existing normal.json fixtures through the pipeline twice instead, per claude-provider-04's own Wave-2 recommendation) | 10h (DAG estimate) | not tracked (no wall-clock instrumentation available in this environment); one continuous pass, no interruption beyond the merge-main step | None beyond golangci-lint itself needing a fresh `brew install` in this environment (not a code/design dependency — the binary was simply not present locally yet) | testdata/provider-events/claude/statusline/compacted.json was not explicitly named in the DAG (which only says "compacted (PreCompact-adjacent)" as a fixture category) — necessary because no existing fixture modeled the post-compaction signature (low context usage + high cumulative cost) | Two self-inflicted test bugs, both caught before reporting complete rather than shipped: (1) an initial `wantEventCount: 0` expectation for statusline/missing_fields.json was wrong — the fixture leaves `context_window_size` non-null even though it nulls every other context/cost field, so `contextEvent` legitimately still fires once; the test's own expectation, not the production normalizer, was the bug. (2) The duplicate-events test's two "independent" Normalizer instances both used a fresh `seqIDs{}` counter starting at 0, so their first-generated EventIDs were coincidentally IDENTICAL — this made the dedup assertion pass for the wrong reason (event_id PRIMARY KEY collision instead of idempotency_key UNIQUE-index dedup) until an explicit "second delivery's EventID must not be independently retrievable" assertion caught it, fixed by seeding the second Normalizer's seqIDs at an offset (mirroring store_test.go's own concurrent-writes test pattern) | Two avoidable retest cycles from the bugs above — both would have been avoided by writing the fixture's exact field-presence shape into the test's expectation as a first step (reading missing_fields.json byte-for-byte before writing wantEventCount, rather than assuming from the file's name) and by never trusting two independently-constructed fakes to produce non-colliding IDs without arranging for it explicitly | 1) **A privacy-gate test should be sanity-checked by proving it fails, not just read for correctness.** This node deliberately injected a real raw-prompt-text leak into normalizer.go, confirmed TestFixture_RawTextNeverPersistedOrLogged failed loudly and specifically (not vacuously) at three levels (Event JSON, Event %#v dump, raw SQLite payload_json column), then reverted the injection and confirmed a clean `git diff`. This cost a few extra minutes and should be standard practice for any "hard privacy gate" / "constitution-level invariant" test in this codebase, not just this one — an assertion that has never been observed to fail is not yet evidence of anything. 2) When two fake/deterministic dependencies (e.g. two `seqIDs{}` instances) are constructed independently in the same test to simulate two "different" real-world callers, explicitly seed them to non-overlapping ranges rather than assuming independent construction implies non-colliding output — this is a general test-hygiene note applicable well beyond this node. |
