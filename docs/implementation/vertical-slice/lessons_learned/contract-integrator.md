# Lessons Learned — contract-integrator (Bootstrap stage)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_preflight |
|---|---|---|---|---|---|---|---|---|---|---|---|
| bootstrap-01 (contract-integrator-01..07 collapsed into one Bootstrap pass) | M (per-task average across contract-integrator-01..07 in the frozen DAG) | L — larger as one continuous unit than the sum of its parts suggested | ~24 (sum of contract-integrator-01..06 estimates) | 16 | n/a — DAG has no duration field; not tracked pre-execution | ~1 continuous execution pass, no wall-clock instrumentation available in this environment | go.mod bootstrap ownership (resolved: lead-only, one-line); Go toolchain version mismatch (1.19.1 vs required 1.26.5, resolved via brew upgrade) | go.mod, internal/domain/status_test.go, pkg/protocol/v1/event_test.go — test files were not separately enumerated as DAG deliverables but were necessary to satisfy "tests pass" in the Completion Definition | 1) go.mod ownership conflicted with the Constitution's own path-ownership rule until the repository owner ruled explicitly. 2) A self-authored test (`TestStatusWireStrings`) initially used a `map[string]string` keyed by wire-string value; `TurnCompleted` and `NodeCompleted` both stringify to `"completed"`, causing a duplicate-key compile failure caught by `go vet`, not by review. | None directly observed in this pass (no multi-agent back-and-forth occurred — this was a single continuous lead execution, not a delegated task with review loops) | 1) The frozen DAG's task granularity (contract-integrator-01 through -07 as 7 separate nodes) does not match how this work is actually executed in practice — it is one continuous, tightly-coupled pass (domain types, ports, protocol, and the freeze document all reference each other and were easiest to validate together, not incrementally per sub-task). Future DAGs for this kind of "define all the shared shapes" work might be better modeled as one node with an internal checklist rather than 7 nodes with formal inter-dependencies. 2) When a DAG models a bootstrap/foundation-defining role, add an explicit note on which specific file (go.mod) is a cross-role blocking dependency, so the ownership conflict is caught during DAG generation, not during Wave 1 kickoff. 3) `go vet`'s duplicate-map-key detection is a good example of "validation commands catch real correctness bugs, not just formatting" — worth keeping `go vet` as a mandatory step in every role's validation command, not an optional one. |
