# 經驗回顧 — claude-provider（Wave: claude-provider-01/02/03）

> 🌐 [English](claude-provider.md) | 繁體中文
>
> 本文件為非規範翻譯，內容以英文版為準（ADR-049）。

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|

## Wave 2

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-04 | L（依 DAG 估算約 6 個檔案） | M-L — 檔案數與估算相符，但一旦解決了設計問題（struct 對 event 的基數對應）之後，整體工作量其實比較接近 M，而非 DAG 估算的 L；工作的主要部分在於設計對應關係，而不是撰寫 JSON marshaling 的程式碼本身 | 6 | 3（`normalizer.go`、`normalizer_test.go`、`privacy_test.go` — DAG 估算的 6 個檔案很可能是預期每個來源 struct 各自對應一個獨立的 normalizer 檔案；但此角色將全部四個整合進單一 `normalizer.go`，因為它們共用 envelope／digestKey 輔助函式，若拆分則勢必得複製這些輔助函式，或再新增第四個純內部用途的檔案來存放它們） | 未追蹤（此環境沒有可用的實際時間量測工具） | 一次連續作業完成，過程無中斷 | 找到並重複使用了 `domain.IDGenerator`／`domain.Clock`（`internal/domain/clock.go`）作為 EventID／時間戳記產生的注入介面，而非依賴 foundation 尚未建置完成的 `internal/idgen`（foundation-06，此節點並不依賴它）——這點在 packet 中並未明確提及，但符合 `CONTRACT_FREEZE.md` 中「Bootstrap 並未凍結……擁有該角色的人員可能會發現需要額外欄位」精神的延伸應用，只是這裡套用在*依賴關係*而非欄位上 | 除 DAG 自身列出的產出物（`normalizer.go`、`normalizer_test.go`、`privacy_test.go`）之外沒有其他檔案 | 無 — contract-integrator-04 的 `pkg/protocol/v1.EventType` 分類體系已經包含所有需要的事件類型（`EventProviderContextObserved`／`UsageObserved`／`QuotaObserved`／`TurnStarted`／`TurnCompleted`／`TurnFailed`／`RateLimitHit`）；沒有缺口需要回報 | 此節點執行過程中未觀察到 | 1) DAG 中「高風險——是從原始 provider payload 進入凍結事件封裝的唯一路徑」的註記，就精神而言是準確的，但實際的風險其實體現為一個*設計*問題（一個 StatusLineSnapshot 究竟要變成幾個事件？），而不是缺少 contract 型別的問題——未來 DAG 的風險欄位值得註明：normalization 節點承擔的是基數對應（cardinality-mapping）風險，而不只是分類完整性風險。2) 重複使用 Wave-1 parser 自身的 fixture（透過在此節點測試中呼叫 `Parse*`），而不是另外新增第四個 telemetry 專屬的 fixture 目錄，讓產出物數量維持在較低水準，也代表 Wave-1 fixture 的變動會自動一併驗證此節點的 normalization 邏輯——建議未來各 phase 中其他 normalization 層的節點都採取相同做法，而不是在上一層重複建立 fixture |
| claude-provider-06 | S | S — 與估算相符；由於是 stub 性質（依 DAG 自身註記，runtime-b01 的 CLI 尚不存在），機械性的工作量不大，但要仔細閱讀 `Auspex_ADD.md` 附錄 E.2／E.3，才能察覺其與 `agents/runtime.md`／DAG 之間 CLI 子指令 PascalCase 對 kebab-case 的落差，儘管檔案數不多，仍需要真正花心思 | 3 | 3（`plugin.json`、`hooks.json`、`README.md`） | 未追蹤 | 一次連續作業完成 | 無 | `README.md` 並未明確列在 DAG 的產出物清單中，但為了記錄此為前瞻性 stub 的狀態，以及 ADD 與 `runtime.md` 之間 CLI 命名風格（casing）的衝突，而不是悄悄自行選一種寫法，因此有必要新增此檔案 | 發現了一個真實存在的跨文件不一致之處：`Auspex_ADD.md` 附錄 E.3 將 CLI 子指令拼寫為 PascalCase（`auspex hook claude UserPromptSubmit`），而 `agents/runtime.md` 的 P0 指令清單，以及 DAG 自身針對這個節點的驗證指令，卻拼寫為 kebab-case（`auspex hook claude user-prompt-submit`）。依任務指示，這尚不足以構成完整的節點阻擋型 STOP（存在一個站得住腳的預設做法：遵循此節點自身已凍結的驗證指令），但仍記錄在本進度產出物與 `integrations/claude/README.md` 中，而不是悄悄自行解決，因為只有 contract-integrator 才能真正協調這個問題 | 未觀察到 | 1) 即使是「S／可接受 stub」等級的節點，仍受益於對照此角色明確擁有的 ADD 章節（附錄 E.2／E.3），而不是只看 DAG／packet——packet 中僅一行提及（「Claude plugin/hooks 範例，呼叫 `auspex hook claude ...`」）完全沒有說明命名風格的問題，而 ADD 則有較完整（雖然目前已與其他文件衝突）的答案。2) 值得由 contract-integrator 進行一次修正，將附錄 E.3 改為 kebab-case，或將 `agents/runtime.md` 的 P0 清單改為 PascalCase——目前兩份近乎凍結的文件在這個細節上互相矛盾，未來的角色（runtime-b01）在實際建置 CLI 時勢必得二選一 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-01 | M | M — 與估算相符；大量使用指標的 struct 結構與巢狀 optional 解碼，所需的細心程度超出程式碼行數所暗示的水準，但並未需要任何新的抽象設計 | 8 | 7（`statusline.go`、`statusline_test.go`，加上 5 個 fixture JSON 檔案——這 5 個 fixture 都是先前一次中斷嘗試留下、尚未納入版本控制的既有檔案，經檢視後予以保留沿用，而非重新撰寫） | 未追蹤（此環境沒有可用的實際時間量測工具） | 一次連續作業完成，過程無中斷 | 無 | 除 DAG 自身列出的產出物之外沒有其他檔案 | 既有的 fixture 在信任其內容之前需要先行檢視——結果證實它們與 ADD §22.5 的欄位清單完全一致，因此不需要重做，但這一點必須經過驗證，而不能只是假設成立 | 此節點執行過程中未觀察到 | DAG 中在 claude-provider-01 底下「需要具代表性的 fixture」這項風險註記是準確的——值得在 DAG 中明確指出（而不只是寫在風險欄位裡）：角色在重複使用遺留／既有 fixture 之前，應一律先檢視其 schema 是否一致，因為一次過時的嘗試同樣有可能是錯的 |
| claude-provider-02 | M | M — 與估算相符。隱私雜湊需求（解析後立即進行 SHA-256，任何地方都不得保留原始文字欄位）在找到並依循 `internal/app/ports.go` 中 `EvaluateTurnRequest.PromptHash` 的命名慣例之後，就變得直截了當 | 6 | 9（`userpromptsubmit.go`、`userpromptsubmit_test.go`，加上 5 個 request fixture 及 2 個 golden response fixture） | 未追蹤 | 一次連續作業完成 | 無 | Golden response 檔案（`response_allow.golden.json`、`response_block.golden.json`）並未明確列在 DAG 的產出物清單中，但為了滿足 packet 中「block/allow response golden files」的測試需求，有必要新增 | ADD 只展示了 block-response 的 JSON 形狀（§22.3），並未明確給出 allow-response 的形狀——因此必須自行做出並記錄一項判斷（使用空的 `{}`，省略 decision 欄位），而非依循一個已凍結的形狀來實作 | 此節點執行過程中未觀察到 | 建議未來由 contract-integrator 進行一次修正，明確凍結 `UserPromptSubmit` 的 allow-response 形狀，否則 claude-provider-06（CLI wrapper，較後面的 phase）以及其他新增類似 hook 的 provider，都將各自獨立做出自己的判斷 |
| claude-provider-03 | M | M — 檔案數與估算相符，但比 01／02 多了一些反覆修正，因為 `classifyFailure` 判斷法則的第一版與其對應的測試 fixture（`unknown_category.json`）在一個狀態碼邊界案例（599）上彼此矛盾，必須加以協調 | 6 | 12（`stop.go`、`stop_test.go`，加上 4 個 Stop fixture 及 7 個 StopFailure fixture——StopFailure 需要比 Stop 更多的 fixture，才能涵蓋 packet 要求的每一個 `domain.FailureClass` 分支） | 未追蹤 | 一次連續作業，加上一次簡短的後續作業（在一次作業中途的中斷之後），用以補上遺漏的 `stop_test.go` 並修正 fixture／classifier 之間的不一致 | 無 | 除 DAG 自身列出的產出物之外沒有其他檔案 | 1) 一次作業階段中斷（harness 層級的速率限制，並非程式碼問題）導致 `stop.go` 在沒有對應測試檔案的狀態下被送出；協調者（coordinator）在回報完成之前，透過獨立驗證發現了這一點——Progress Tree 的紀律（沒有證據就不相信「完成」）在此確實發揮了作用，只是這次是由外部把關，而不是此角色自身的審閱步驟。2) `unknown_category.json` 的第一版包含 `status_code: 599`，而 `classifyFailure` 本身的 5xx 區間備援規則正確地將其對應到 `FailureProviderInternal`，而非 `FailureUnknown`——換句話說，錯的是 fixture 本身宣稱要測試的內容，而不是 classifier，後來透過移除該狀態碼修正，讓這個 fixture 真正涵蓋到未對應錯誤類型的路徑 | 輕微：發現上述 fixture／classifier 不一致之處，浪費了一次測試執行循環——若能先以註解寫下 classifier 的判斷表，再依此推導出 fixture，而不是憑直覺同時撰寫 fixture 與 classifier，即可避免 | 1) 當一項任務同時涉及分類判斷法則，以及用來驗證其各分支的手動建立 fixture 時，應先寫出分支對照表作為單一事實來源，再據此同時推導出程式碼的 switch/case 與 fixture——可降低 fixture 與實作各自獨立地「一致同意彼此不一致」的機率。2) 從協調者的角度來看，harness 層級的節點中途中斷，與角色自行悄悄提早停止並無法區分——協調者以獨立的 `gofmt`／build／`vet`／test 驗證來因應（而不是信任角色的自我回報），是正確的緩解做法，應維持為標準作業方式，而不只是這次事件的權宜之計 |

## Wave 4

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-05 | M（依 DAG 估算：約 300 行程式碼，約 6 個檔案） | M — 與 DAG 的複雜度區間相符；schema 設計上的判斷（該寫入哪一個或哪些資料表，鑑於 ADD §12.2 列出了四個可能的候選對象——`events`／`turns`／`turn_usage`／`quota_observations`／`context_observations`，而此角色自身的 normalizer 目前只會產生通用的 `v1.Event` 值，尚未產生 turn／usage 形狀的 struct）比寫入路徑本身更費心思，一旦確定目標資料表之後，寫入路徑就相當機械化 | 6 | 3（`migrations/0010_events.sql`、`store.go`、`store_test.go`——比估算少，因為通用的 `events` 資料表統一吸收了此角色所有已 normalize 的事件類型；若採用 turns／turn_usage／quota_observations／context_observations 各自獨立資料表的設計，就需要每個資料表各自一份 migration 加一份 store 檔案，而不是總共各一份） | 300（DAG 估算） | 未追蹤（此環境沒有可用的實際時間量測工具）；一次連續作業完成，過程無中斷 | **任務本身步驟 1（開始前先合併 main）浮現出一個真實且具有實質重要性的依賴關係**：此節點若沒有 foundation-06 的 `internal/storage/sqlite` package（`DB.WithTx`、`QuerierFromContext`、`Migrate`、`AllMigrations`）及其 0001-0004 migration 存在於此分支上，就完全無法編譯或測試——在 Wave-4 合併之前，`vertical-slice/claude-provider` 分支上完全不存在這些東西（此分支 Wave 1/2 的工作，也就是 claude-provider-04，當初刻意只依賴 `internal/domain` 的 Clock／IDGenerator port，而不依賴任何具體的儲存引擎，正是為了不受制於 foundation 的時程——參見 claude-provider-04 的 Wave 2 經驗條目）。合併本身是一次乾淨的 fast-forward（分支 HEAD 原本就已經是 `origin/main` 的祖先），沒有任何衝突，因此這裡的「非預期依賴」其實完全在任務說明的預期之內，而不是實作過程中發現的意外 | 除上述三項列出的產出物之外沒有其他檔案 | **一個真實存在、雖然範圍不大的 SQLite 語法陷阱**：針對*部分（partial）*唯一索引（`CREATE UNIQUE INDEX ... WHERE idempotency_key IS NOT NULL`，這是 ADD §12.3 本身逐字要求的索引 SQL）執行 `INSERT ... ON CONFLICT(col) DO NOTHING`，除非 INSERT 自身的 ON CONFLICT 子句重複完全相同的 WHERE 條件，否則會在執行期失敗（「ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint」）。這一點無法透過閱讀 schema 或 Go driver 文件事先發現——只有在第一個 idempotency 測試實際針對真實的（暫存檔案）SQLite 資料庫執行時才會浮現。成本：一次完整的測試執行／修正／重測循環，僅幾分鐘，並非結構性的阻礙。**第二項、影響更為重大的發現**：在新增 `migrations/0010_events.sql` 之後，`internal/storage/sqlite/migrate_test.go` 中三個既有測試（`TestAllMigrations_LoadsCoreSchemaFiles`、`TestCoreMigrations_FromEmptyDatabase`、`TestCoreMigrations_ReopenFromFile_AppliesOnce`）開始失敗，因為它們把「恰好存在 4 個 migration」／「`CurrentVersion == 4`」寫死。這個檔案不在此角色的專屬路徑範圍內（由 foundation 而非 claude-provider 擁有），因此未在此處修正——而是依 Constitution §4 rule 4，改為在進度產出物中記錄為一項阻礙。這是 foundation 自身測試隔離上的*結構性*缺口（任何第二個落地 migration 範圍的角色都會觸發相同的寫死計數斷言），並非此節點 schema 選擇所特有的問題，值得大聲提出，因為 checkpoint／predictor／runtime 一旦各自落地自己的第一個 migration，就會遇到完全相同的失敗，除非 foundation／contract-integrator 在那之前把 `migrate_test.go` 的斷言修改為與範圍無關（例如「至少包含這些具名資料表」而非「`len(migrations) == N`」） | 輕微：上述 ON CONFLICT／partial-index 語法問題造成一次原本可避免的重測循環；現已記錄在 `store.go` 自身的文件註解中，讓未來任何角色的讀者都不必再獨立重新發現一次 | 1) **提醒 contract-integrator／foundation**：`internal/storage/sqlite/migrate_test.go` 中寫死的 migration 數量（`len(migrations) == 4`、`CurrentVersion == 4`）會導致往後每一個新增自己 migration 範圍的角色（checkpoint 0020-0039、predictor 0040-0049、runtime 0050-0059）都出問題——建議在更多角色各自獨立撞上同一道牆之前，把這些斷言改為檢查具名資料表是否存在，而非檢查確切的數量／版本。2) DAG 針對儲存寫入類節點的每節點程式碼行數／檔案數估算（此節點為：300 行程式碼／6 個檔案），似乎是假設每個邏輯資料表都各自對應一組檔案（migration + store）；當某角色實際的資料模型可以乾淨地收斂成單一共用資料表時（如此角色的四種事件子類型透過通用的 `events` log 那樣），該估算應被視為上限，而非目標——為了「符合估算」而刻意依子類型拆分檔案，只會增加複雜度而沒有相對應的效益。3) 建議將全倉庫範圍的 `go test ./...`（而不只是節點自身範圍限定的驗證指令）納入任何新增 migration 檔案節點的標準最後一步，原因正是上述跨角色的 `migrate_test.go` 連帶影響——節點自身範圍限定的驗證指令（`go test ./internal/telemetry/claude/... -run Idempotent`）並不會單獨揭露這個問題。 |

## Wave 5

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|
| claude-provider-07 | L（依 DAG 估算：300 點，約 10 小時） | M-L — DAG 自身的風險註記（「中度——raw-prompt 不存在的斷言是一道嚴格的隱私關卡」）才是真正準確的成本驅動因素：一旦理解了既有的 normalizer／store 介面，撰寫 table-driven 的 fixture-pipeline 測試本身相當機械化，但要設計一個值得信賴的隱私關卡測試（真正具有可證偽性，而不只是恰好會通過的測試）則需要真正的深思熟慮 | 2（依 DAG 自身大約 2 個檔案的估算，雖然 DAG 並未針對此節點明確給出檔案數） | 2（`internal/telemetry/claude/fixture_suite_test.go`、`testdata/provider-events/claude/statusline/compacted.json`）——刻意*沒有*新增一個用於重複事件的 fixture 檔案（而是依循 claude-provider-04 在 Wave-2 自身提出的建議，改為讓既有的 `normal.json` fixture 在 pipeline 中重複跑兩次） | 10 小時（DAG 估算） | 未追蹤（此環境沒有可用的實際時間量測工具）；一次連續作業完成，除了合併 main 的步驟之外沒有其他中斷 | 除了 golangci-lint 本身在此環境中需要重新以 `brew install` 安裝之外，沒有其他依賴（這不是程式碼／設計層面的依賴——只是該執行檔在本機尚未安裝而已） | `testdata/provider-events/claude/statusline/compacted.json` 並未明確列在 DAG 中（DAG 只將「compacted（近似 PreCompact）」列為一個 fixture 類別）——之所以有必要新增，是因為沒有任何既有 fixture 能模擬 compaction 之後的特徵訊號（低 context 使用量加上高累積成本） | 兩個自己造成的測試錯誤，都在回報完成之前被抓到，而非直接送出：(1) 針對 `statusline/missing_fields.json` 最初設定的 `wantEventCount: 0` 預期值是錯的——該 fixture 雖然把其他所有 context／cost 欄位都設為 null，卻讓 `context_window_size` 保持非 null，因此 `contextEvent` 理當仍會觸發一次；錯的是測試自身的預期值，而不是正式環境的 normalizer。(2) 重複事件測試中兩個「各自獨立」的 Normalizer 實例，都使用一個從 0 開始的全新 `seqIDs{}` 計數器，導致它們各自第一個產生的 EventID 恰好*相同*——這使得去重（dedup）的斷言雖然通過，理由卻是錯的（是 `event_id` PRIMARY KEY 衝突，而不是 `idempotency_key` UNIQUE 索引去重），直到加入一項明確的斷言「第二次傳送的 EventID 不應能被獨立取得」才揭穿了這一點，修正方式是讓第二個 Normalizer 的 `seqIDs` 從一個偏移量開始（呼應 `store_test.go` 自身在並行寫入測試中的模式） | 上述錯誤造成了兩次原本可避免的重測循環——兩者都可以透過以下方式避免：第一步就先把 fixture 精確的欄位存在形狀寫進測試的預期值中（先逐位元組閱讀 `missing_fields.json` 再撰寫 `wantEventCount`，而不是從檔名去推測），以及絕不假設兩個各自獨立建構的假物件（fake）會自動產生不衝突的 ID，除非明確安排如此 | 1) **隱私關卡測試應該透過證明它會失敗來做健全性檢查，而不只是靠閱讀程式碼來確認正確性。** 此節點刻意在 `normalizer.go` 中注入一個真實的 raw-prompt 文字外洩，確認 `TestFixture_RawTextNeverPersistedOrLogged` 會明確且具體地失敗（而不是空洞地失敗），且在三個層級都是如此（Event JSON、Event 的 `%#v` dump、SQLite 中原始的 `payload_json` 欄位），接著還原該注入並確認 `git diff` 乾淨。這只多花了幾分鐘，且應該成為此程式碼庫中任何「嚴格隱私關卡」／「constitution 層級不變性」測試的標準作業方式，不只限於這一個——一項從未被觀察到失敗過的斷言，尚不能構成任何證據。2) 當同一測試中獨立建構兩個假的／確定性的相依物件（例如兩個 `seqIDs{}` 實例）用來模擬兩個「不同」的真實呼叫端時，應明確將它們設定於不重疊的範圍，而不是假設各自獨立建構就代表輸出不會衝突——這是一項適用範圍遠超出此節點的通用測試衛生原則。 |
