# foundation — 經驗回顧

> 🌐 [English](foundation.md) | 繁體中文
>
> 本文件為非規範翻譯，內容以英文版為準（ADR-049）。

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|
| foundation-01 | S | S（符合預估） | 4 | 9（7 個新的實作／測試檔 + 修改 go.mod + 新建 go.sum） | EXECUTION_DAG.md 中未估計（只給了 LOC／複雜度，沒有時間單位） | 約 25 分鐘 wall-clock（閱讀治理文件、實作、驗證、撰寫 artifacts、commit） | github.com/google/uuid、github.com/spf13/cobra、github.com/spf13/pflag（transitive）、github.com/inconshreveable/mousetrap（transitive）— 全部都已由任務簡報本身預先指明，所以所謂「unexpected」只在這個意義上成立：DAG 自己的 foundation-01 該列只把「Go module path decision if no Git remote yet」列為唯一具名的 blocker，完全沒提到要加入哪些套件；是任務簡報解決了這件事，DAG 沒有 | go.sum（機械性產物，永遠伴隨 go.mod 的變更 — 照理說每個動到 go.mod 的 node 都應該把它算進去，但 DAG 的 estimated_files_changed 欄位看起來並沒有把它單獨計入） | 無 — go.mod 已因 Bootstrap 例外而存在，worktree 是乾淨的，所有 frozen contracts（domain.Clock、domain.IDGenerator）都毫無歧義，完全不需要任何詮釋 | 無明顯浪費；任務簡報異常完整（明確指定了確切的 UUID 函式庫、確切的驗證指令與確切的交付項目清單），因此不需要為釐清歧義而來回重讀來源文件 | (1) EXECUTION_DAG.md 對 foundation-01 的「Est. Files: 4」，一旦把 buildinfo/clock/idgen/main 每一項的實作檔與其必要的測試檔都算進去，低估幅度超過 2 倍 — 「每個可測試行為單位 2 個檔案」的經驗法則會預測 8 而不是 4，更接近實際寫出的 7 個新檔案；估算器應假設測試檔是額外的檔案，而不是折進實作估計裡。(2) DAG 給了 LOC 與檔案數的預估，但對 foundation 任務完全沒有 duration 預估或單位（不像營建業的 Gantt chart 那樣）— 如果 Auspex 自己的 scope estimator 要能預測 duration，這個資料點在源頭就是缺的，在 DAG 格式加入 duration 欄位或 LOC 換算 duration 的 heuristic 之前，無法拿實際 duration 來驗證。(3) 「S / 120 LOC / 4 files」這組 tuple 低估了一件事：一個 vertical-slice CLI stub node 還得新增並釘住兩個真實的第三方相依（含 transitive deps）並執行 go mod tidy — 相依導入（dependency-onboarding）的成本（即使像這裡一樣輕微）似乎在複雜度量表 XS/S/M/L/XL 的定義中被隱含地當成免費，而那些定義只看 LOC。 |
| foundation-02 | S | S（符合預估） | 3 | 4（paths.go、env.go、fake_env_test.go、paths_test.go） | 180（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 20 分鐘 wall-clock | 無 — 不需要新的 go.mod 相依，只用 stdlib（errors、path、runtime、strings） | fake_env_test.go 從 paths_test.go 獨立出來成為單獨檔案（為了可讀性把 test double 隔離在主測試檔之外；DAG 的「3 files」預估相當接近：它大概算的是 paths.go + env.go + 一個測試檔，沒有預期到會把 fake 拆成自己的檔案） | 必要的跨平台路徑表格測試抓到一個真實的 bug：第一版實作在 Windows 分支使用 filepath.Join，而它用的是「宿主 OS」的分隔符（在這台 darwin 開發機上執行時，Windows 路徑被產成正斜線），而不是正在解析的「目標 OS」的分隔符。修法是寫一個使用字面反斜線的專用 winJoin helper，並把 POSIX 分支改用 path.Join（永遠是正斜線）取代 filepath.Join（依宿主而定）— 這正是 DAG 自己 foundation-02 風險註記（「Windows path behavior needs CI matrix」）預先警告的那一類 bug；一個 fake-GOOS 的單元測試就抓到了它，不需要真正的 CI matrix | 為了確認這個 package 的 scope 邊界（只管全域目錄，不管 repo 內的 .auspex/**）而重讀 Auspex_ADD.md SS26.1/SS26.3，花的時間比寫程式本身還久 — ADD 的文字把「.auspex/config.yaml」和「global user config」放在同一個 precedence 清單裡，卻沒有明說哪個 package 擁有哪一個，所以 scope 只能從 agents/foundation.md 的措辭（「OS-correct config/data/cache/runtime paths」）加上 Constitution SS7 第 10 條規則（不要建造目前 milestone 不需要的抽象）推斷出來，而不是被直接陳述 | (1) filepath.Join 對上宿主 GOOS 是一類微妙、容易漏掉的 bug，專屬於任何要為「非其編譯／執行所在 GOOS」解析路徑的 package — 值得在 agents/foundation.md 或一份共用的 Go style 備註中明確點出，供未來任何做類似跨平台解析的角色參考，因為 `go vet`/`gofmt` 抓不到它，它只會透過測試中針對特定 OS 的 assertion 才浮現。(2) DAG 的 foundation-02 風險欄在抽象層面正確預測了失效模式（「Windows path behavior needs CI matrix」），但實際的修正完全不需要 CI matrix，只需要一個以 GOOS 為參數的 Resolve() 函式和 fake Env — 未來的估算可以注意：把 OS 選擇器本身（而不只是環境）做成依賴注入，能把「需要 CI matrix」的風險變成同一台主機上的單元測試，成本嚴格更低。 |
| foundation-03 | M | S-M（符合預估，甚至可說比 M 簡單） | 4 | 5（config.go、errors.go、config_test.go、go.mod、go.sum） | 250（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 25 分鐘 wall-clock | go.yaml.in/yaml/v3 從 indirect（由 spf13/cobra 傳遞引入）升為 direct — 就「選了一個新函式庫」的意義而言並非「unexpected」（Auspex_ADD.md SS1.4 寫的是「YAML + JSON Schema」，沒有指名特定的 Go 函式庫），但值得標註的是：在這個 node 動到它之前，它早已躺在 go.sum 裡，是在寫任何程式碼之前透過 `go list -m all \| grep yaml` 發現的 | 除了原本就預期的 go.mod/go.sum 這一對之外沒有 | 最大的時間成本是一個 scope 決策，而不是技術問題：任務簡報明確警告不要發明目前沒有任何東西在消費的欄位，但 ADD SS26.4 提供了一棵完整、誘人、隨時可以照抄的預設 config YAML 樹（runtime/privacy/prediction/risk/state_checkpointing/repository_checkpoint/graceful_pause）。最後選擇讓 Config.Raw 維持為 generic map，外加一份已知頂層 key 的 allowlist（僅用於偵測未知欄位），而不是為全部 7 個 section 建立 typed structs，因為 repo 中目前沒有任何 package 會讀取任何 config 欄位 — predictor/policy/checkpoint/runtime 都還不存在。這是一個未來角色會實際撞上的、承重（load-bearing）的設計選擇（見 docs/implementation/vertical-slice/foundation.md 中的 assumptions），不是小細節 | 無 | (1) EXECUTION_DAG.md 的 M/250-LOC/4-files 預估（事後看來正確地）假設了這個 phase「不會」建出完整的 ADD SS26.4 struct 樹 — 實際檔案數（算入 go.mod/go.sum 後為 5）與 LOC 之所以貼近預估，正是因為 scope 被限縮在載入／優先序／驗證引擎，而非完整的 typed schema，這印證了任務簡報「keep this minimal and honest」的指示是正確判斷，而非過度保守。(2) 一個值得為後續角色記錄的先例：當 ADD 提供了一個完整示範的範例（像 SS26.4 的 YAML 區塊），但尚未有任何會讀取它的程式碼支撐時，應把該範例視為「最終形狀的文件記載」，而不是當前 node 的實作 checklist — 否則每個碰到「已有文件記載但尚無程式碼消費」規格的「M」node，都會膨脹成把整份規格照抄一遍。 |
| foundation-04（縮減範圍：僅 internal/lock） | S（已從 DAG 原本的 S/150/6 縮減，因為 3 個子範圍中的 2 個 — clock、idgen — 已在 foundation-01 完成） | S（符合預估） | 未單獨估計（任務簡報給了縮減後的估計，但沒有給出獨立的檔案數） | 4（lock.go、process_unix.go、process_windows.go、lock_test.go） | 150（DAG 原本、縮減前針對「完整」clock/idgen/lock 範圍的 LOC 預估 — 與實際縮減範圍後的工作並非同一基準的比較） | 約 20 分鐘 wall-clock | 無 — 只用 stdlib（errors、os、os/exec、strconv、strings，以及 unix build 上的 syscall） | 透過 build tags 拆分出 process_unix.go / process_windows.go — 沒有任何 DAG 列的文字預期到這點，但一旦設計需要真正的跨平台存活（liveness）檢查就無可避免（os.FindProcess 在 POSIX 與 Windows 之間的行為差異大到單一共用實作必然會在其中一邊出錯） | 無 — internal/domain 或 internal/app/ports.go 中都不存在任何 frozen 的 Lock 介面（開工前已用 grep 驗證），所以沒有需要對齊的 contract，只有任務本身明確把機制選擇委派出來這件事 | 帶 Windows tag 的 process_windows.go 無法在這台 darwin 開發主機上執行／測試以驗證 — 那一側的正確性建立在有文件記載的 Go stdlib 語意上（os.FindProcess 在 Windows 上會開啟真實的 handle，且對不存在的 PID 會失敗），而不是一個實際執行過的測試；在 qa-01 的 Windows CI matrix 實際跑到它之前，這是一個真實的覆蓋缺口 | (1) 當一個任務縮減了某個已被估算過的 DAG node 的範圍（就像這裡的 foundation-04 拿掉了 clock/idgen），DAG 原本的 S/150-LOC/6-files 數字就從有用的訊號變成誤導性的雜訊 — 未來的 DAG 維護應該為縮減後的 node 重新發布一份修正過的估計，或明確把不再適用的欄位歸零，而不是把過時的完整範圍數字留在「縮減範圍」指示旁邊。(2) 只能在本機單一 OS 上實際執行的跨平台原語（這裡是帶 windows tag 的檔案），正是 qa-01 的跨平台 CI matrix 需要相對於這類 node 提早執行的理由 — 目前的 phase 順序允許多個帶有未測試 Windows 分支的 foundation node（foundation-02 的路徑表、foundation-04 的行程存活檢查）不斷累積，直到某個 CI matrix 真的在真實 Windows 上編譯／執行它們為止。 |
| foundation-05 | M | M（符合預估，但 High 風險旗標名符其實） | 5 | 6（db.go、migrate.go、db_test.go、migrate_test.go、go.mod、go.sum） | 350（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 45 分鐘 wall-clock（本 phase 最久的 node — High 風險旗標正確預測了需要更謹慎的工作） | modernc.org/sqlite 自身的 transitive 樹（modernc.org/libc、ccgo/v4、cc/v4、goabi0 等）— 規模很大，但 ADD SS1.4 早已做出的 pure-Go／no-CGO driver 選擇已完全預期到；不是驚喜，只是 `go mod tidy` 的輸出比先前的 node 更重 | go.sum 大幅增長（pure-Go 的 SQLite driver 會拉進相當於一整套 C transpiler 工具鏈的支援套件）— 就 driver 的選擇而言在預期之內，不是設計錯誤 | 本 node 承重的發現：internal/app.TxFunc 的 frozen 簽名是 `func(ctx context.Context) error`，「沒有」*sql.Tx 參數，這是一個真實的限制（ports.go 由 contract-integrator 擁有且已 frozen），迫使交易交接改採以 context key 為基礎的方式（QuerierFromContext），而不是一個從零開始的 storage 層通常會採用、更直覺的「把 *sql.Tx 直接傳給 callback」設計。要讓編譯期 assertion（`var _ app.TxRunner = (*DB)(nil)`）真的能編譯，WithTx 的簽名必須使用具名的 `app.TxFunc` 型別，而不是結構上等價的 inline func 型別 — Go 在介面滿足性的 method-set 比對這裡需要的是精確的具名型別 | 透過實際執行 PRAGMA 查詢以及一個真實的並發寫入者 busy-timeout 情境來驗證 pragmas（而不只是斷言 Open() 回傳 nil），花了實打實的設計時間，但這是 agents/foundation.md 的必要測試清單與任務指示中對走捷徑的明確警告兩者都明文要求的 — 考量到這個 node 的 High 風險旗標（「load-bearing for every later role」），時間花得值得 | (1) EXECUTION_DAG 的風險欄（「Medium — WAL/busy-timeout/FK pragmas are load-bearing for every later role」）是本 phase 所有 node 中最準確的風險預測 — 這裡若 pragma 設計錯誤（例如只信任 DSN 編碼而不加驗證，或不去測試 busy_timeout 實際的阻塞行為），對之後每個角色的 storage 程式碼都會是無聲、難以診斷的問題，而不是建置失敗。建議未來的 DAG 在橫切的基礎設施 node 上保留這種明確的「load-bearing for every later role」風險註記。(2) internal/app/ports.go 的 TxFunc 形狀（沒有 *sql.Tx 參數）是每個未來要寫 store 的角色都必須知道的限制；現在已特別記錄在 docs/implementation/vertical-slice/foundation.md 的 Handoff 一節，讓 checkpoint/predictor/claude-provider/runtime 不必各自獨立地重新發現一次。 |
| foundation-09 | XS | S（略高於 XS — 工具安裝與真實 lint 問題的修正，讓耗時實際超出撰寫 3 個設定檔的範圍） | 6 | 12（新增 Taskfile.yml、Makefile、.golangci.yml；9 個既有檔案只因首次啟用 golangci-lint 所浮現的真實 lint 修正而被動到） | 150（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 35 分鐘 wall-clock（比 XS/150-LOC 預估所暗示的更久，幾乎完全是因為要從零安裝 task/golangci-lint、然後真的去修它們找到的問題，而不是撰寫那 3 個設定檔） | task（go-task/task）與 golangci-lint 都沒有預先安裝；`brew install` 直接失敗（過舊的 Xcode CLT 擋住了 bottle 安裝，是與本 repo 無關、主機上既有的問題），兩者只好改從各自上游的 module path 用 `go install` 安裝 — 相對於 DAG 該列 unexpected-dependencies 欄的「None」，這是貨真價實的意外，不過對任何驗證指令指名了一個不屬於 Go 工具鏈本身的工具（`task`）的 node 來說，或許可以說是可預見的 | 除了兩個 build 檔 + 設定檔之外沒有；被動到但原本就存在的檔案數（9）才是真正的重點 — 如果把這個 node 的「files changed」欄讀成「僅計新檔案」，就低估了這件事 | 沒有阻斷性問題，但本 phase 首次啟用 golangci-lint 在 foundation-02 到 foundation-05 的程式碼中浮現了 16 個真實問題（測試 defer 中未檢查的錯誤、該用 %w 卻用了 %v、該用 errors.Is 卻用了 ==、一個刻意為之但會讓 linter 起疑的 nilerr 案例、兩個落在 Wave-1 檔案上的冗餘型別 staticcheck 提示）— 沒有一個是有實質後果的既有 bug，但全部都得逐一分類處理（修正、或有正當理由的 nolint），而不是一律停用，這是真實且正確的工作，但不是「寫設定檔」 |  | (1) 這是本 phase 中 DAG 預估（XS/150 LOC/6 files/「None」unexpected deps）低估 node 真實成本最明顯的案例：撰寫那 3 個設定檔「的確」大約是 XS 規模，但驗證指令本身（`task lint && task build`）有一個隱藏的前提 — 一套能運作的 `task` 與 `golangci-lint` 安裝 — 而 DAG 的預估隱含地把它假設掉了。任何未來的「lint config」node 都應該把在全新機器上 bootstrap 工具的時間編入預算，而不只是設定檔的撰寫。(2) 在一個跨多個 phase 的 codebase 上第一次立起真正的 lint，本質上是回溯性的 — 它評估的是同一個 phase 中先前 node 在較寬鬆審視下寫出的每一個檔案，而不只是這個 node 自己的新檔案。這是預期中且正確的（在第一個 lint-config node 就把問題抓出來，比讓未來的 qa-01/CI-matrix node 從另一個角色、歷史脈絡更少的情境中抓到要好），但這代表這個 node 真正的 diff 足跡是「整個 phase 中被 lint 修正動到的 Go 檔案」，而不只是 3 個新設定檔，而 DAG 的每 node 檔案數預估沒有任何方式能表達這種回溯驗證的形狀。 |
| foundation-06 | M | M（符合預估，但 High 風險旗標第二次名符其實 — 見所發現的 bug） | 10 | 6（新增 0001_repositories.sql、0002_worktrees.sql、0003_provider_sessions.sql、0004_tasks.sql；修改 migrate.go 與 migrate_test.go） | 300（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 40 分鐘 wall-clock（schema 的轉寫很快；version-0 bug 的診斷 — 從「資料表不見了」一路 bisect 到一個我沒料到會出錯的檔案裡的單一 off-by-one 比較 — 佔了大部分時間） | 無 — 沒有新的 go.mod 相依；embed.FS 是 stdlib | 除了 node 自身範圍已預期的 4 個 migration 檔 + 2 個被修改的引擎／測試檔之外沒有 | 真實的 BUG，自己造成、也自己抓到：把 4 個 migration 編號為 0000-0003（為了在視覺上對齊該範圍自己的「0000-0009」標籤）無聲地弄壞了 migration 0（repositories），因為 migrate.go 的 `Migrate()` 以 `currentVersion() == 0` 作為「尚未套用任何東西」的 sentinel，並以 `m.Version <= current` 做比較 — 於是一個字面上就叫 Version 0 的 migration，在全新的資料庫上會被讀成「已套用」而被跳過且沒有任何錯誤，同時 CurrentVersion() 仍愉快地回報最高版本，彷彿一切都跑過了。之所以被抓到，只因為測試是透過 sqlite_master 斷言資料表真實存在，而不是只信任 CurrentVersion()。修法是重新編號為 0001-0004，而這其實正是 ADD §12.5 早已強制規定的（「Migration file: 0001_name.sql」），也是這份進度 artifact 自己的 foundation-05 Handoff 一節早已記載的 — 所以這個 bug 本來只要在為檔案命名前更仔細地重讀自己團隊既有的文件就能避免，並不是真正的 unknown-unknown | 診斷過程本身（把「資料表沒建立但沒有錯誤、而且 CurrentVersion 說已套用」一路收斂到單一個 `<=` 比較）需要在該 package 裡撰寫並丟棄 6 個一次性的除錯測試檔，逐一把 embed 對比字面 SQL、有註解對比無註解、單一 migration 對比完整集合、version-0 對比 version-1 當成獨立假設來 bisect — 每一個單獨看都很便宜，但來回的總量是這個 node 最大的單項時間成本，比撰寫實際的 schema 或真正的測試還大 | (1) 在 off-by-one 的版本比較上「無聲跳過且不報錯」是比硬性錯誤更糟的失效模式 — CurrentVersion() 回報「已套用 4 個」而實際只存在 3 張資料表，正是 Constitution §6/CONTRACT_FREEZE.md 的 fail-closed 規則所要防止的那種狀態完整性缺口，即使這個特定案例是在開發階段而非正式環境被抓到。未來的某個 foundation node（不在本 phase，目前超出範圍）可以考慮讓 Migrate() 防禦性地直接拒絕任何 Version <= 0 的 migration，或讓 currentVersion() 用 nullable/sentinel 型別區分「migrations 資料表沒有任何列」與「明確處於 version 0」，而不是讓整數 0 同時承載兩種意義 — 這點已標註在進度 artifact 的 assumptions 中而非在此修正，因為在沒有其他角色的需求驅動下更改 migrate.go 的 contract，感覺是超出 foundation-06 簡報範圍的 scope creep。(2) 當治理文件（這裡是：本檔案自己既有的 Handoff 一節，在 foundation-05 期間寫下）已經明確陳述了一項慣例（「Migration file: 0001_name.sql」），為了「與範圍標籤在外觀上一致」而偏離它，就不是一個中性的風格選擇 — 在偏離已陳述的慣例之前，先重讀自己團隊已經寫好的文件，即使那個偏離看起來無害。 |
| foundation-08 | S | S（符合預估） | 4 | 1（internal/config/precedence_paths_test.go，新檔 — 純測試 node，未更動任何 production 程式碼） | 200（DAG 僅有 LOC 預估，沒有 duration 單位） | 約 20 分鐘 wall-clock（本 phase 最快的 node；兩個底層 package 從 foundation-02/03 起就已穩固，不需任何更動，只需要新測試） | 無 — 沒有新的 go.mod 相依；兩個 package 都已完整建好 | 除了 node 自身範圍已預期的單一個新的跨 package 測試檔之外沒有；DAG 的「Est. Files: 4」相對於實際值（1）偏高，很可能是因為 DAG 無法事先知道兩個底層 package 完全不需要 production 更動、整個 node 會是集中在單一檔案裡的純測試撰寫 | 一個真實但很快修好的測試撰寫錯誤（與 foundation-06 不同，不是 production bug）：TestPrecedence_PathsResolvedGlobal_LosesToRepoAndEnvLayers 的初稿完全漏掉了一個帶有 schema_version 的 layer，正確地觸發了 config.Load 既有的（foundation-03）「merged result 中必須有 schema_version」驗證 — 修法是加上一個明確的 defaults layer，與 config_test.go 自身既有測試已在使用的模式一致；這證實 config.go 的驗證有在做事，而不是它的缺口 | 無明顯浪費；這是本 phase 最順利的 node — 開工前仔細閱讀 paths.go/env.go/config.go/config_test.go/paths_test.go（為了找出真正的覆蓋缺口，而不是重複既有的 precedence 測試）花的時間比實際撰寫那 3 個新測試還久 | (1) DAG 的驗證指令（`go test ./internal/paths/... ./internal/config/... -run Precedence`）在 internal/paths 中本身匹配不到任何測試 — paths 既有帶 precedence 性質的測試命名為 TestResolve_*_XDGOverrides，而非 *Precedence*，所以這個過濾條件永遠只會選到 config package 的測試（包含既有的 TestLoad_Precedence_* 與這個 node 新增的跨 package 測試）。這不是這個 node 工作上的 bug，但它意味著 DAG 所載的驗證指令在撰寫時假設了兩個 package 的測試名稱都會包含「Precedence」，而這個命名慣例實際上並不存在於 foundation-02 的 paths_test.go 裡 — 值得為未來的 DAG 撰寫標註：node 的驗證指令與其目標 package 的實際測試命名慣例應該互相核對，而不是只憑 package 名稱寫出來。(2) 當兩個 package 各自在孤立情況下都已有良好覆蓋時（如 foundation-02/03 之後的 paths 與 config），新增跨 package 測試真正有價值的切入點，是去問「一個『真實呼叫者』實際上會怎麼同時使用這兩者」，而不是試圖在任一 package 內部再發明更多 precedence 排列組合 — 這條務實的 pipeline（paths.Resolve 決定「在哪裡」，config.Load 決定「哪些 bytes 勝出」）正是任一 package 自己的測試套件從內部都看不到的接縫，也正是一個在孤立 worktree 中工作的角色自然會留給後續橫切 node 去補上的那種缺口。 |
| foundation-07 | M（被框定為「test harness 強化」，紙面上是 S 規模） | L（找到並修正兩個真實的 production bug，不只是新增測試） | 1（migrate_test.go，純新增） | 3（除了測試檔之外，migrate.go 與 db.go 都被修改；migrate.go 的 Migrate() 實質上被重寫） | 沒有帶 duration 單位的估計（Wave 4 的任務指示給的是範圍，不是 LOC／檔案數） | 約 70 分鐘 wall-clock（自 foundation-05/06 的 High 風險 node 以來最久的一個；大部分時間花在 probe test 的 bisect，而不是撰寫最終的測試） | 無 — 沒有新的 go.mod 相依；唯一新增的 import（modernc.org/sqlite 的頂層 package，別名為 `modernc`）本來就是 foundation-05 起既有的 direct 相依，只是先前 db.go 本身沒有 import 它（僅透過 driver 那個具副作用的 blank import 被引用） | 一旦找到那些 bug 之後，除了已預期的 3 個檔案之外沒有；沒有建立新檔案 | 沒有阻斷性問題，但這個 node 的實際形狀與它「強化 test harness」的框定嚴重分歧：寫下的「第一個」新測試（並發重開）就立刻在 Migrate() 中找到一個真實的 TOCTOU race（foundation-05/06 的設計把讀取目前版本與套用 migrations 當成彼此不同步的操作），而修正那個 race（透過 BEGIN IMMEDIATE）又立刻暴露出「第二個」不相關的真實 bug — Open() 自己的 pragma bootstrap 在如今持有更久的寫入鎖之下可能以 SQLITE_BUSY 失敗，在 `-race -count=20` 下每次執行約有 5-30% 的 flake 率。兩個 bug 都不是假設性或理論性的；兩者都先透過一次性的 probe 測試檔重現，才真正動手修正 | 對 SQLITE_BUSY bootstrap race 的 probe／bisect 花了實打實、刻意投入的時間：必須先排除「這是不是 Go 層級的 data race」（不是 — race detector 全程乾淨，這是 SQLite 層級的鎖時序 race）、「這是不是 pragma 順序問題」（部分是 — 把 busy_timeout 排到最前面是必要的，但單靠它「並不」充分，只做那個修正後仍然會 flake），最後才落在真正的根因：「全新連線上的第一個陳述式還沒有任何 busy_timeout 保護，因為 busy_timeout 要等到『它自己』成功之後才生效」，需要的是一個 retry 迴圈，而不只是重新排序。閱讀 modernc.org/sqlite 自己的原始碼（其 sqlite.go 中的 applyQueryParams）以確認 driver 在 DSN 解析中本來就會優先處理 busy_timeout，是必要的一步，才能停止追逐一個 driver 早已排除的理論 | (1) 一個針對 High 風險、橫切引擎（其他每個角色的 storage 都仰賴的 SQLite migration／連線程式碼）而被框定為「test harness 強化」的 node，在估算時就應假設它「可能」會找到並需要修正真實的並發 bug，而不是只在 production 程式碼不變的前提下撰寫新測試檔 — 這已是完全相同的模式第二次出現在本檔案中（foundation-06 名義上只是「撰寫 migration 檔案」，卻找到並修正了一個真實的 version-0 sentinel bug），這暗示基礎設施 node 上的「hardening」／「strengthen coverage」框定，應隱含地預期可能有小規模、範圍內的引擎修正，而不只是測試新增，並據此估算（更像 Medium/High 風險 node，而不是 DAG 隱含的「只是加測試」規模）。(2) 這裡的並發 bug 對 `go test -race` 本身是隱形的 — Go 的 race detector 在整個 node 期間抓到的 Go 層級 data race 是零，因為這個 race 完全發生在 SQLite 自身的鎖機制內部（一條連線的讀取與另一條連線的寫入之間的 TOCTOU，以資料庫檔案為媒介，而非共享的 Go 記憶體）。這值得對任何未來撰寫並發 storage 測試的角色明確標註：`-race` 是必要的，但對於抓多連線的 SQLite 層級 race 遠遠不夠；抓到這類 bug 的唯一方法，是一個真的驅動 N 個真實並發 Open()+Migrate() 呼叫打在同一個檔案上、並對「結果」做斷言的測試（重複的資料列、收斂的版本、沒有無聲的部分套用），這正是 TestMigration_ConcurrentReopen_SerializesAndConverges 及其姊妹測試所做的事。(3) 把 Migrate() 修改為單一整批交易（用 BEGIN IMMEDIATE ... COMMIT 包住整段「先讀取、再套用 N 個 migrations」的序列，取代每個 migration 一筆交易）是比這個 node 開工時的框定所暗示的更大的行為變更 — 它還免費修好了「第三個」更隱微的問題（一批 [好的 migration, 壞的 migration] 過去會把好的那個永久 commit，即使 Migrate() 對整批回傳了錯誤，這是 CONTRACT_FREEZE.md 的錯誤 contract 明文禁止的部分套用狀態完整性缺口）。值得為未來的 DAG 估算記下：修正一個循序操作引擎裡的並發 bug，往往自然也會修好一個原子性 bug，因為兩者源自同一個根本設計缺口（多筆彼此獨立的交易，而不是一筆將其整個包覆的交易）— 這兩者並不是彼此獨立的估算項目。 |
