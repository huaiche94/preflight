# foundation — Lessons Learned

> 🌐 English | [繁體中文](foundation.zh-TW.md)

| task_id | estimated_complexity | actual_complexity | estimated_files_changed | actual_files_changed | estimated_duration | actual_duration | unexpected_dependencies | unexpected_files | blockers_encountered | token_waste_observations | recommendations_for_auspex |
|---|---|---|---|---|---|---|---|---|---|---|---|
| foundation-01 | S | S (held) | 4 | 9 (7 new impl/test files + go.mod modified + go.sum created) | not estimated in EXECUTION_DAG.md (only LOC/complexity given, no time unit) | ~25 min wall-clock (read governing docs, implement, validate, write artifacts, commit) | github.com/google/uuid, github.com/spf13/cobra, github.com/spf13/pflag (transitive), github.com/inconshreveable/mousetrap (transitive) — all anticipated by the task brief itself, so "unexpected" only in the sense that the DAG's own foundation-01 row lists "Go module path decision if no Git remote yet" as its sole named blocker and says nothing about which packages to add; the brief resolved this, the DAG did not | go.sum (mechanical, always follows go.mod changes — arguably should be counted in every node touching go.mod but the DAG's estimated_files_changed columns don't appear to count it separately) | none — go.mod already existed from the Bootstrap exception, worktree was clean, all frozen contracts (domain.Clock, domain.IDGenerator) were unambiguous and required zero interpretation | none of significance; the task brief was unusually complete (it specified the exact UUID library, the exact validation commands, and the exact deliverable list), so there was no back-and-forth re-reading of source docs to resolve ambiguity | (1) EXECUTION_DAG.md's "Est. Files: 4" for foundation-01 undercounts by more than 2x once you include both the impl file and its required test file for each of buildinfo/clock/idgen/main — a rule of thumb of "2 files per unit of testable behavior" would have predicted 8 instead of 4, closer to the 7 new files actually written; the estimator should assume test files are additional files, not folded into the impl estimate. (2) The DAG gives LOC and file estimates but no duration estimate/unit at all for foundation tasks (unlike, say, a construction Gantt chart) — if Auspex's own scope estimator is meant to predict duration, this data point is missing at the source and can't be validated against actual duration until the DAG format adds a duration column or LOC-to-duration heuristic. (3) The "S / 120 LOC / 4 files" tuple undersells that a vertical-slice CLI stub node also has to add and pin two real third-party dependencies (with transitive deps) and run go mod tidy — dependency-onboarding cost (even when trivial, as here) seems to be implicitly assumed as free in the complexity scale's XS/S/M/L/XL definitions, which are LOC-only. |
| foundation-02 | S | S (held) | 3 | 4 (paths.go, env.go, fake_env_test.go, paths_test.go) | 180 (DAG LOC estimate only, no duration unit) | ~20 min wall-clock | none — no new go.mod dependency needed, stdlib only (errors, path, runtime, strings) | fake_env_test.go as a separate file from paths_test.go (kept the test double out of the main test file for readability; DAG's "3 files" estimate landed close: it likely counted paths.go + env.go + one test file and didn't anticipate splitting the fake into its own file) | one real bug caught by the required cross-platform path-table tests: the first implementation used filepath.Join for the Windows branch, which uses the HOST OS's separator (produced forward slashes for Windows paths when run on this darwin dev machine) instead of the TARGET OS's separator being resolved. Fixed by writing a dedicated winJoin helper using literal backslashes and switching the POSIX branches to path.Join (always forward slash) instead of filepath.Join (host-dependent) — this is precisely the class of bug the DAG's own foundation-02 risk note ("Windows path behavior needs CI matrix") was pre-warning about; a fake-GOOS unit test caught it without needing an actual CI matrix | re-reading Auspex_ADD.md SS26.1/SS26.3 to confirm this package's scope boundary (global dirs only, not repo-local .auspex/**) took longer than writing the code itself — the ADD text names ".auspex/config.yaml" and "global user config" in the same precedence list without explicitly saying which package owns which, so scope had to be inferred from agents/foundation.md's wording ("OS-correct config/data/cache/runtime paths") plus Constitution SS7 rule 10 (don't build abstractions the current milestone doesn't need) rather than being stated directly | (1) filepath.Join-vs-host-GOOS is a subtle, easy-to-miss bug class specific to any package that resolves paths for a GOOS other than the one it's compiled/running on — worth calling out explicitly in agents/foundation.md or a shared Go style note for any future role doing similar cross-platform resolution, since `go vet`/`gofmt` do not catch it and it only surfaces via an OS-specific assertion in a test. (2) The DAG's foundation-02 risk column correctly predicted the failure mode in the abstract ("Windows path behavior needs CI matrix") but the actual fix needed no CI matrix at all, just a GOOS-parameterized Resolve() function and fake Env — future estimates could note that dependency-injecting the OS-selector itself (not just the environment) turns an "needs CI matrix" risk into a same-host unit test, which is strictly cheaper. |
| foundation-03 | M | S-M (held, arguably simpler than M) | 4 | 5 (config.go, errors.go, config_test.go, go.mod, go.sum) | 250 (DAG LOC estimate only, no duration unit) | ~25 min wall-clock | go.yaml.in/yaml/v3 promoted from indirect (pulled in transitively by spf13/cobra) to direct — not "unexpected" in the sense of a new library choice (Auspex_ADD.md SS1.4 names "YAML + JSON Schema" without naming a specific Go library), but worth flagging that it was already sitting in go.sum before this node touched it, discovered via `go list -m all \| grep yaml` before writing any code | none beyond the go.mod/go.sum pair already anticipated | the biggest time cost was a scope decision, not a technical one: the task brief explicitly warned against inventing fields nothing consumes yet, but ADD SS26.4 provides a full, tempting, ready-to-transcribe default-config YAML tree (runtime/privacy/prediction/risk/state_checkpointing/repository_checkpoint/graceful_pause). Chose to keep Config.Raw as a generic map plus a known-top-level-keys allowlist (for unknown-field detection only) rather than typed structs for all 7 sections, since zero packages in the repo currently read any config field — predictor/policy/checkpoint/runtime don't exist yet. This is a real, load-bearing design choice future roles will hit (see assumptions in docs/implementation/vertical-slice/foundation.md), not a minor detail | none | (1) EXECUTION_DAG.md's M/250-LOC/4-files estimate assumed (correctly, in hindsight) that a full ADD SS26.4 struct tree would NOT be built this phase — the actual file count (5, counting go.mod/go.sum) and LOC landed close to estimate specifically because scope was kept to the load/precedence/validation engine and not the full typed schema, confirming the task brief's "keep this minimal and honest" instruction was the right call, not over-caution. (2) A precedent worth recording for later roles: when ADD provides a fully worked example (like SS26.4's YAML block) that is NOT yet backed by any code that reads it, treat the example as documentation of eventual shape, not as an implementation checklist for the current node — otherwise every "M" node that touches a documented-but-unconsumed spec balloons into transcribing the whole spec. |
| foundation-04 (reduced scope: internal/lock only) | S (already reduced from DAG's original S/150/6, since 2 of 3 sub-scopes — clock, idgen — were already done in foundation-01) | S (held) | not separately estimated (task brief gave a reduced estimate but no distinct file count) | 4 (lock.go, process_unix.go, process_windows.go, lock_test.go) | 150 (DAG's original, pre-reduction LOC estimate for the FULL clock/idgen/lock scope — not apples-to-apples with the actual reduced-scope work) | ~20 min wall-clock | none — stdlib only (errors, os, os/exec, strconv, strings, syscall on unix build) | process_unix.go / process_windows.go split via build tags — not anticipated by any DAG row text, but unavoidable once the design needs a real cross-platform liveness check (os.FindProcess behaves differently enough between POSIX and Windows that a single shared implementation would be wrong on one side) | none — no frozen Lock interface existed anywhere in internal/domain or internal/app/ports.go (verified via grep before starting), so there was no contract to reconcile against, only the task's own explicit delegation of the mechanism choice | verifying the Windows-tagged process_windows.go could not be run/tested on this darwin dev host — correctness there rests on documented Go stdlib semantics (os.FindProcess opens a real handle on Windows and fails for a nonexistent PID) rather than an executed test, which is a real coverage gap until qa-01's Windows CI matrix exercises it | (1) When a task reduces an already-estimated DAG node's scope (as foundation-04 did here, dropping clock/idgen), the DAG's original S/150-LOC/6-files numbers become misleading noise rather than useful signal — future DAG maintenance should either re-issue a corrected estimate for the reduced node or explicitly zero out the fields that no longer apply, rather than leaving stale full-scope numbers next to a "reduced scope" instruction. (2) Cross-platform primitives that can only be exercised on one OS locally (here: the windows-tagged file) are an argument for why qa-01's cross-platform CI matrix needs to run early relative to nodes like this one — the current phase order lets multiple foundation nodes with untested Windows branches (foundation-02's path table, foundation-04's process liveness check) accumulate before any CI matrix actually compiles/runs them on real Windows. |
| foundation-05 | M | M (held, but High risk flag earned its keep) | 5 | 6 (db.go, migrate.go, db_test.go, migrate_test.go, go.mod, go.sum) | 350 (DAG LOC estimate only, no duration unit) | ~45 min wall-clock (longest node this phase — High risk flag correctly predicted more careful work was needed) | modernc.org/sqlite's own transitive tree (modernc.org/libc, ccgo/v4, cc/v4, goabi0, etc.) — large but fully anticipated by the pure-Go/no-CGO driver choice ADD SS1.4 already made; not a surprise, just a heavier `go mod tidy` output than earlier nodes | go.sum grew substantially (pure-Go SQLite driver pulls a C-transpiler toolchain's worth of supporting packages) — expected given the driver choice, not a design mistake | the load-bearing discovery this node: internal/app.TxFunc's frozen signature is `func(ctx context.Context) error` with NO *sql.Tx parameter, which is a real constraint (ports.go is contract-integrator-owned and frozen) that forced a context-key-based transaction handoff (QuerierFromContext) instead of the more obvious 'pass *sql.Tx directly to the callback' design a from-scratch storage layer would normally use. Getting the compile-time assertion (`var _ app.TxRunner = (*DB)(nil)`) to actually compile required using the named `app.TxFunc` type in WithTx's signature, not a structurally-equivalent inline func type — Go's method-set matching for interface satisfaction needs the exact named type here | verifying pragmas by executing PRAGMA queries and a real concurrent-writer busy-timeout scenario (not just asserting Open() returns nil) took real design time, but was explicitly required by both agents/foundation.md's required-tests list and the task instruction's explicit warning against the shortcut — time well spent given this node's High risk flag ("load-bearing for every later role") | (1) The EXECUTION_DAG's risk column ("Medium — WAL/busy-timeout/FK pragmas are load-bearing for every later role") was the most accurate risk prediction of any node this phase — a wrong pragma design here (e.g. trusting DSN encoding alone without verifying it, or not testing busy_timeout's actual blocking behavior) would have been a silent, hard-to-diagnose problem for every subsequent role's storage code, not a build failure. Recommend keeping this kind of explicit 'load-bearing for every later role' risk annotation on cross-cutting infrastructure nodes in future DAGs. (2) internal/app/ports.go's TxFunc shape (no *sql.Tx param) is a constraint every future role writing a store must know about; this is now documented in the Handoff section of docs/implementation/vertical-slice/foundation.md specifically so checkpoint/predictor/claude-provider/runtime don't each independently rediscover it. |
| foundation-09 | XS | S (held slightly above XS — tool installation and real lint-issue fixups added real time beyond writing 3 config files) | 6 | 12 (Taskfile.yml, Makefile, .golangci.yml new; 9 pre-existing files touched only for real lint fixes surfaced by turning on golangci-lint for the first time) | 150 (DAG LOC estimate only, no duration unit) | ~35 min wall-clock (longer than the XS/150-LOC estimate implies, almost entirely due to installing task/golangci-lint from scratch and then actually fixing what they found, not writing the 3 config files) | task (go-task/task) and golangci-lint were not preinstalled; `brew install` failed outright (outdated Xcode CLT blocking bottle installs, a pre-existing host issue unrelated to this repo) and both had to be `go install`'d from their upstream module paths instead — genuinely unexpected relative to the DAG row's "None" in the unexpected-dependencies column, though arguably foreseeable for any node whose validation command names a tool (`task`) that isn't part of the Go toolchain itself | none beyond the two build files + config; the touched-but-pre-existing file count (9) is the real story this node's "files changed" column undercounts if read as "new files only" | none blocking, but turning on golangci-lint for the first time this phase surfaced 16 real issues across foundation-02 through foundation-05's code (unchecked errors in test defers, %v-instead-of-%w, == instead of errors.Is, one deliberate-but-linter-suspicious nilerr case, two redundant-type staticcheck hints on Wave-1 files) — none were pre-existing bugs of consequence, but all had to be triaged (fix vs. justified nolint) one at a time rather than blanket-disabled, which is real, correct work but not "config file writing" |  | (1) This is the clearest case this phase of a DAG estimate (XS/150 LOC/6 files/"None" unexpected deps) undercounting a node's true cost: writing the 3 config files WAS roughly XS-sized, but the validation command itself (`task lint && task build`) has a hidden prerequisite — a working `task` and `golangci-lint` installation — that the DAG's estimate implicitly assumed away. Any future 'lint config' node should budget for tool bootstrap time on a fresh machine, not just the config authoring. (2) Standing up real lint for the first time on a multi-phase codebase is inherently retroactive — it evaluates every file written under looser scrutiny in earlier nodes this same phase, not just this node's own new files. That's expected and correct (catching the issues here, at the first lint-config node, is better than a future qa-01/CI-matrix node catching them from a different role's context with less history), but it means this node's real diff footprint is 'the whole phase's Go files touched by lint fixes,' not just the 3 new config files, and the DAG's per-node file-count estimate has no way to represent that retroactive-verification shape. |
| foundation-06 | M | M (held, but High-risk flag earned its keep a second time — see the bug found) | 10 | 6 (0001_repositories.sql, 0002_worktrees.sql, 0003_provider_sessions.sql, 0004_tasks.sql new; migrate.go and migrate_test.go modified) | 300 (DAG LOC estimate only, no duration unit) | ~40 min wall-clock (schema transcription was fast; the version-0 bug's diagnosis — bisecting from "table missing" down to a single off-by-one comparison in a file I didn't expect to be wrong — was most of the time) | none — no new go.mod dependency; embed.FS is stdlib | none beyond the 4 migration files + the 2 modified engine/test files already anticipated by the node's own scope | REAL BUG, self-inflicted then self-caught: numbering the 4 migrations 0000-0003 (to visually match the range's own "0000-0009" label) silently broke migration 0 (repositories) because migrate.go's `Migrate()` uses `currentVersion() == 0` as its "nothing applied yet" sentinel and compares `m.Version <= current` — so a migration literally named Version 0 reads as "already applied" on a brand-new database and is skipped with no error, while CurrentVersion() still happily reports the highest version as if everything ran. Only caught because the test asserted the table's real existence via sqlite_master instead of trusting CurrentVersion() alone. Fixed by renumbering to 0001-0004, which is also literally what ADD §12.5 already mandates ("Migration file: 0001_name.sql") and what this same progress artifact's own foundation-05 Handoff section already documented — so the bug was avoidable by re-reading my own team's existing docs more carefully before naming files, not a genuine unknown-unknown | the diagnosis process itself (isolating "table not created but no error and CurrentVersion says applied" down to a single `<=` comparison) required writing and discarding 6 throwaway debug test files in the package to bisect embed-vs-literal-SQL, comments-vs-no-comments, single-migration-vs-full-set, and version-0-vs-version-1 as separate hypotheses one at a time — each was cheap individually but the total back-and-forth was the single biggest time cost of this node, larger than writing the actual schema or the real tests | (1) A silent skip-with-no-error on an off-by-one version comparison is a worse failure mode than a hard error would have been — CurrentVersion() reporting "4 applied" while only 3 tables actually exist is exactly the kind of state-integrity gap Constitution §6/CONTRACT_FREEZE.md's fail-closed rule exists to prevent, even though this particular instance was caught in dev, not production. A future foundation node (not this phase, out of scope now) could consider having Migrate() defensively reject any migration with Version <= 0 outright, or having currentVersion() distinguish "no migrations table row" from "explicitly at version 0" using a nullable/sentinel type rather than overloading integer 0 for both meanings — flagged in the progress artifact's assumptions rather than fixed here, since changing migrate.go's contract without a driving need from another role felt like scope creep beyond foundation-06's brief. (2) When a governing doc (here: this file's own pre-existing Handoff section, written during foundation-05) already states a convention ("Migration file: 0001_name.sql") explicitly, deviating from it "for cosmetic consistency with a range label" is not a neutral style choice — re-read your own team's already-written docs before deviating from a stated convention, even when the deviation looks harmless. |
| foundation-08 | S | S (held) | 4 | 1 (internal/config/precedence_paths_test.go, new — test-only node, no production code changed) | 200 (DAG LOC estimate only, no duration unit) | ~20 min wall-clock (fastest node this phase; both underlying packages were already solid from foundation-02/03 and needed no changes, only new tests) | none — no new go.mod dependency; both packages already fully built | none beyond the single new cross-package test file already anticipated by the node's own scope; the DAG's "Est. Files: 4" landed high relative to actual (1), likely because the DAG couldn't know in advance that both underlying packages would need zero production changes and the whole node would be pure test-writing in one file | one real, quickly-fixed test-authoring mistake (not a production bug, unlike foundation-06): the first draft of TestPrecedence_PathsResolvedGlobal_LosesToRepoAndEnvLayers omitted a schema_version-bearing layer entirely, correctly tripping config.Load's pre-existing (foundation-03) "schema_version required in merged result" validation — fixed by adding an explicit defaults layer, matching the pattern config_test.go's own existing tests already use; confirms config.go's validation is doing its job, not a gap in it | none of significance; this was the smoothest node of the phase — reading paths.go/env.go/config.go/config_test.go/paths_test.go closely up front (to find the genuine coverage gap rather than duplicating existing precedence tests) took longer than writing the actual 3 new tests | (1) The DAG's validation command (`go test ./internal/paths/... ./internal/config/... -run Precedence`) matches zero tests in internal/paths on its own — paths' existing precedence-flavored tests are named TestResolve_*_XDGOverrides, not *Precedence*, so the filter only ever selects config-package tests (both the pre-existing TestLoad_Precedence_* ones and this node's new cross-package ones). This is not a bug in this node's work, but it means the DAG's stated validation command was written assuming test names in both packages would contain "Precedence" without that naming convention actually existing in paths_test.go from foundation-02 — worth flagging for future DAG-writing so a node's validation command and the actual test-naming convention of the packages it targets are checked against each other, not just written from the package names. (2) A genuinely useful place to add cross-package tests when two packages are each well-covered in isolation (as paths and config both were, from foundation-02/03) is to ask "what does a REAL CALLER actually do with both of these together" rather than trying to invent more precedence permutations within either package alone — the realistic pipeline (paths.Resolve decides WHERE, config.Load decides WHICH BYTES WIN) was the seam neither package's own test suite could see from inside itself, and is exactly the kind of gap an isolated-worktree role naturally leaves for a later cross-cutting node to close. |
| foundation-07 | M (framed as "test harness hardening," S-sized on paper) | L (two real production bugs found and fixed, not just tests added) | 1 (migrate_test.go, additive) | 3 (migrate.go and db.go both modified beyond the test file; migrate.go's Migrate() effectively rewritten) | not estimated with a duration unit (Wave 4 task instruction gave scope, not LOC/files) | ~70 min wall-clock (longest node since foundation-05/06's High-risk nodes; most of the time was probe-test bisection, not writing the final tests) | none — no new go.mod dependency; the one import added (modernc.org/sqlite's top-level package, aliased `modernc`) was already an existing direct dependency from foundation-05, just not previously imported by db.go itself (only referenced via the driver's side-effecting blank import) | none beyond the 3 files already anticipated once the bugs were found; no new files created | none blocking, but the node's actual shape diverged sharply from its "harden the test harness" framing: the FIRST new test written (concurrent reopen) immediately found a real TOCTOU race in Migrate() (foundation-05/06's design read current-version and applied migrations as unsynchronized operations), and fixing that race (via BEGIN IMMEDIATE) immediately exposed a SECOND, unrelated real bug — Open()'s own pragma bootstrap could fail with SQLITE_BUSY under the now-longer-held write lock, at a ~5-30%-per-run flake rate under `-race -count=20`. Neither bug was hypothetical or theoretical; both were reproduced via throwaway probe test files before being fixed for real | probing/bisecting the SQLITE_BUSY bootstrap race took real, deliberate time: had to rule out "is this a Go-level data race" (no — race detector was clean throughout, this was a SQLite-level lock-timing race), "is this pragma ordering" (partially — reordering busy_timeout-first was necessary but NOT sufficient on its own, still flaked after that fix alone), before landing on "the very first statement on a brand-new connection has no busy_timeout protection yet, because busy_timeout only takes effect once IT succeeds" as the real root cause requiring a retry loop, not just reordering. Reading modernc.org/sqlite's own source (applyQueryParams in its sqlite.go) to confirm the driver already prioritizes busy_timeout in DSN parsing was necessary to stop chasing a theory the driver had already ruled out | (1) A node scoped as "test harness hardening" against a High-risk, cross-cutting engine (SQLite migration/connection code every other role's storage depends on) should be estimated assuming it MAY find and need to fix real concurrency bugs, not just author new test files against unchanged production code — this is the second time this exact pattern has occurred in this file (foundation-06 found and fixed a real version-0 sentinel bug while nominally just "writing migration files"), suggesting "hardening"/"strengthen coverage" framing on infrastructure nodes should carry an implicit expectation of possible small in-scope engine fixes, not just test additions, and be estimated accordingly (more like a Medium/High-risk node than the DAG's implied "just add tests" sizing). (2) The concurrency bug here was invisible to `go test -race` itself — the Go race detector caught zero Go-level data races throughout this entire node, because the race was entirely inside SQLite's own locking (a TOCTOU between one connection's read and another's write, mediated by the database file, not shared Go memory). This is worth flagging explicitly for any future role writing concurrent storage tests: `-race` is necessary but nowhere near sufficient for catching multi-connection SQLite-level races; the only way to catch this class of bug is a test that actually drives N real concurrent Open()+Migrate() calls against one file and asserts on the RESULT (duplicate rows, converged version, no silent partial application), which is exactly what TestMigration_ConcurrentReopen_SerializesAndConverges and its sibling do. (3) Fixing Migrate() to be one whole-batch transaction (BEGIN IMMEDIATE ... COMMIT around the entire read-then-apply-N-migrations sequence, replacing one-transaction-per-migration) was a bigger behavior change than the node's framing suggested going in — it also fixed a THIRD, more subtle issue for free (a batch of [good migration, bad migration] used to permanently commit the good one even though Migrate() returned an error for the batch, a partial-application state-integrity gap CONTRACT_FREEZE.md's error contract explicitly forbids). Worth noting for future DAG estimation: fixing a concurrency bug in a sequential-operation engine often naturally also fixes an atomicity bug, because both stem from the same root design gap (multiple independent transactions instead of one enclosing one) — the two are not independent estimation line items. |
