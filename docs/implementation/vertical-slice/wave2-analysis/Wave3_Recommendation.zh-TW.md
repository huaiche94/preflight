# Wave 3 建議

> 🌐 [English](Wave3_Recommendation.md) | 繁體中文
>
> 本文件為非規範翻譯，內容以英文版為準（ADR-049）。

| 欄位 | 值 |
|---|---|
| 階段 | 3.10 — Wave 2 後分析 |
| 狀態 | **僅為建議。尚未指派任何隊友。尚未執行。等待核准。** |
| 假定的前提條件 | 本分析將 Wave 2 的四個分支視為已整合（依 DAG 相依語意），因為它們已個別完成驗證與確認，且依本階段自身的定義屬於不可變更的 Wave 2 紀錄。**它們實際上尚未合併進 `main`** — 該合併本身是一項待辦動作，本文件並不執行、也未獲授權執行該動作。 |
| 使用的輸入 | `docs/implementation/vertical-slice/EXECUTION_DAG.md`（已凍結）、`Auspex_Predictor_Design_Supplement.md`、`Prediction_Error_Report.md`、`Missing_Telemetry_Report.md`、`Feature_Registry.md`、`Prediction_Confidence_Report.md`、`Feature_Gap_Report.md`、`ADR_Recommendations.md` |

## 1. 新解鎖的節點

透過對照每個尚未完成的 DAG 節點的相依清單，與已完成節點的完整集合（Bootstrap + Wave 1 + Wave 2 = 19 個節點）比對後判定。有六個節點屬於新解鎖，或仍未指派但已解鎖：

| 節點 | 相依項目 | 是否全部滿足？ | 本 phase 新解鎖，或先前已解鎖但從未指派？ |
|---|---|---|---|
| `foundation-06` | `foundation-05`、`contract-integrator-01` | 是（`foundation-05` 已於 Wave 2 完成） | 本 phase 新解鎖 |
| `foundation-08` | `foundation-02`、`foundation-03` | 是（兩者皆已於 Wave 2 完成） | 本 phase 新解鎖 |
| `predictor-05b` | `predictor-05` | 是（已於 Wave 2 完成） | 本 phase 新解鎖 |
| `runtime-b01` | `contract-integrator-07`、`foundation-01` | 是（兩者皆已於 **Wave 1** 完成） | 自 Wave 1 結束後便已解鎖 — 因「runtime」從未是 Wave 1／2 四位具名隊友之一，故從未被指派 |
| `qa-01` | `foundation-09`、`contract-integrator-07` | 是（`foundation-09` 已於 Wave 2 完成） | 本 phase 新解鎖 |
| `qa-08` | —（無相依項目） | 是（空泛地成立） | 自 Bootstrap 起便已解鎖 — 因「qa」從未是 4 位具名隊友之一，故從未被指派 |

**這件事本身就值得明白指出：** 有兩個節點（`runtime-b01`、`qa-08`）自 Wave 1 結束後便可執行，卻始終未被拿起處理，純粹是因為團隊名冊（4 位隊友：`foundation`、`claude-provider`、`checkpoint`、`predictor`）從未納入 `runtime` 或 `qa`。這不是 DAG 的問題 — 而是儲存庫擁有者在 Wave 1–2 刻意做出的團隊組成決策 — 但 Wave 3 規劃應將其視為一項需要再次明確做出的選擇，而不是應被悄悄修正的疏漏。

## 2. 仍受阻的節點（為求完整列出 — 並非整個 DAG 的窮舉，僅限直接前緣節點）

| 節點 | 受阻於 |
|---|---|
| `foundation-07` | `foundation-06` |
| `claude-provider-05` | `foundation-06` |
| `claude-provider-07` | `claude-provider-05` |
| `checkpoint-a01` | `foundation-06` |
| `checkpoint-b01` | `foundation-06` |
| `checkpoint-b04` | `checkpoint-b01` |
| `predictor-01` | `foundation-06` |
| `predictor-05c` | `predictor-05b` |
| `predictor-07` | `predictor-05c` |
| `runtime-a01` | `foundation-06` |
| `runtime-b02` | `foundation-06` |

**觀察：** 光是 `foundation-06` 就直接阻擋了這 11 個節點中的 7 個 — 它是整個剩餘圖中的主要瓶頸，而不只是眾多選項之一。這正是 §4 排名的依據。

## 3. 已解鎖節點的逐節點估計

每一格都明確陳述其來源。若某數值為推導（Derived），則其推導方式與不確定性都會直接寫在該格內 — 這些數字沒有任何一個是憑空捏造的。

### foundation-06

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG 估計：10。校準調整後：約 15（10 × 1.544，`Calibration_Report.md` §1 的平均變更檔案數比率） | DAG：已觀察。調整值：推導 | 低至中（支撐 1.544 比率的樣本數 n=17，單一儲存庫） | 依該比率自身的分佈範圍（觀察到的 1.0×–2.25× 範圍），±5 個檔案屬合理範圍 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 不適用 — 任何節點皆從未測量過此項（`Missing_Telemetry_Report.md` B3） |
| LOC | DAG 估計：300。校準調整後：約 627（300 × 2.089，`Calibration_Report.md` §2 的平均 LOC 比率） | DAG：已觀察。調整值：推導 | 低至中（樣本數 n=13，100% 呈同一方向但個別分佈範圍很寬） | 分佈很寬 — 此節點自身的風險標記（「負載承重……schema 錯誤會連鎖擴散」）顯示，相較於一般 M 節點，它可能需要更謹慎、因而也更長的程式碼 |
| 工期 | 未知 | 未知 | 0.0 | 不存在 DAG 估計（`Prediction_Error_Report.md` §0）；本資料集中沒有與此節點風險程度相當、帶有*高風險*標記的 M 複雜度參照類別 — `foundation-05`（M、高風險、自我回報約 45 分鐘）是最接近的類比，但本身也只是單一資料點 |
| Token 使用量 | 未知 | 未知 | 0.0 | 除 `foundation-01`／`checkpoint-b03` 外，先前沒有任何節點在此細緻程度上存在逐節點 token 資料，且兩者都不是高風險 migration-schema 節點的良好類比 |
| 複雜度 | M（DAG） | 已觀察 | 高（DAG 標籤） | 兩份獨立的經驗教訓條目（`foundation-05`、本 phase 的 Calibration Report）都顯示，被標記為高風險的 M 節點，相較於一般 M 節點往往耗時較長 |
| 執行風險 | 高 — DAG 原文：「每個功能角色的 migration 都以外鍵參照這些資料表……schema 錯誤會連鎖擴散到 claude-provider／checkpoint／predictor／runtime 的 migration 範圍」 | 已觀察（DAG 文字） | 高度信心這確實是高風險，而非只是被貼上標籤 — `foundation-05`（最接近的類比，同一作者角色、同一子系統）已證實確實需要謹慎的 pragma／transaction 設計 | 這是已解鎖集合中槓桿效益最高、風險也最高的單一節點 |

### foundation-08

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG：4。調整後：約 6（4 × 1.544） | 已觀察／推導 | 低至中 | ±2 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 與所有節點相同 |
| LOC | DAG：200。調整後：約 418（200 × 2.089） | 已觀察／推導 | 低至中 | 分佈很寬 |
| 工期 | 約 22 分鐘（推導自本 phase S 複雜度自我回報的參照類別平均：`foundation-01` 約 25 分鐘、`foundation-02` 約 20 分鐘、`foundation-04` 約 20 分鐘） | 推導（較弱 — 來源資料為自我回報，n=3） | 低 | ±10 分鐘屬合理範圍 |
| Token 使用量 | 在逐節點細緻程度上未知（會與該隊友在 Wave 3 被指派的其他工作共用同一筆總量） | 未知 | 0.0 | 不適用 |
| 複雜度 | S（DAG） | 已觀察 | 高（DAG 標籤；此為小型、範圍界定清楚的優先序測試節點，符合本 phase 中 S 節點估計值維持不變的模式） | 低 |
| 執行風險 | 低 — DAG 原文；明確依賴跨平台測試（「需要 Windows／macOS／Linux CI（`qa-01`）才能取得完整訊號」） | 已觀察 | 中 — `Calibration_Report.md` §6 將跨平台工作標記為一項較弱（n=2）的隱藏風險假設，超出名義標籤所示 | 此節點自身的 DAG 註記將 `qa-01` 列為取得*完整*訊號所需的相依項目，即使它並非硬性阻擋相依 — 若兩者在同一 phase 中，值得安排在 `qa-01` 之後執行 |

### predictor-05b（Token 預測器）

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG：4。調整後：約 6（4 × 1.544） | 已觀察／推導 | 低至中 | ±2 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 不適用 |
| LOC | DAG：400。調整後：約 836（400 × 2.089） | 已觀察／推導 | 低至中 | 分佈很寬；此節點自身的風險標記顯示，相較於一般 L 節點，可能需要更多因設計反覆而產生的 LOC（參照 `predictor-05` 填補 cold-start 表格缺口的成本，屬於類似的「設計反覆」模式） |
| 工期 | 未知 | 未知 | 0.0 | **在這整個 19 個節點的資料集中，沒有任何一個 L 複雜度節點擁有乾淨、未受中斷的自我回報工期** — `checkpoint-b02`（L）在工作階段中途被中斷，沒有乾淨的實際時鐘紀錄。這裡確實沒有可供推導的參照類別；在此處給出數字將形同捏造。 |
| Token 使用量 | 未知 | 未知 | 0.0 | 與工期同樣的理由 |
| 複雜度 | L（DAG） | 已觀察 | 高 | `predictor-06`（原本也標記為 L）自我評估後認為實際上是 M — 依 `Calibration_Report.md` §3 的發現，ADD §15 傾向於將一個小型、未校準的 fallback 層級與規模大得多的已校準層級合併在同一個標籤之下，因此此節點也有被過度標記的真實可能性。`predictor-05b` 自身的範圍（ADD §15.1–15.2 的 MVP 公式）依同樣模式屬於較小的層級 — 這使得它更接近 M 而非 L 是說得通的，但這只是一項假設，而非確定的發現，因為 predictor-05b 從未被實際建置過 |
| 執行風險 | 高 — DAG 原文：「輸入 RiskCombiner 的配額／情境風險項；此處若出現系統性偏差，將傳播到每一個下游的策略決策」 | 已觀察 | 高度信心此風險框架是正確的 — token 預測偏差確實會在凍結流程（ADR-041）的每個後續階段中持續累加放大 | 這是一個對正確性至關重要的節點，而不僅僅是一個規模大的節點 |

### runtime-b01（CLI 骨架）

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG：6。調整後：約 9（6 × 1.544） | 已觀察／推導 | 低至中 | ±3 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 不適用 |
| LOC | DAG：350。調整後：約 731（350 × 2.089） | 已觀察／推導 | 低至中 | 分佈很寬 |
| 工期 | 約 30 分鐘（推導自本 phase M 複雜度自我回報的參照類別平均：`foundation-03` 約 25 分鐘、`foundation-05` 約 45 分鐘、`checkpoint-b03` 約 20 分鐘 — 分佈範圍很寬，此平均值僅為較弱的集中趨勢估計） | 推導（較弱，n=3，範圍 20–45 分鐘） | 低 | ±15 分鐘屬合理範圍 — 比 S 複雜度參照類別的範圍更寬 |
| Token 使用量 | 未知 | 未知 | 0.0 | 不適用 |
| 複雜度 | M（DAG） | 已觀察 | 中 | 此節點明確不能觸碰 `cmd/auspex/main.go`（依 vertical-slice 計畫，屬於 `contract-integrator`／`foundation` 所有）— 這代表其實際合併需要超出一般單一角色 M 節點的負責人協調成本，而這項成本並未被 DAG 的複雜度標籤所涵蓋 |
| 執行風險 | 低 — DAG 原文 | 已觀察 | 中 — 根目錄接線協調的需求是一項真實、DAG 未能顯現的整合風險，即使 DAG 自身的風險欄位寫的是「低」，這與 `Calibration_Report.md` 發現 DAG 風險標籤有時會漏掉「整合形」風險（相對於「實作形」風險）的情況類似 | 建議將此視為低至中，而非單純的低 |

### qa-01（CI 基礎建置）

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG：4。調整後：約 6（4 × 1.544） | 已觀察／推導 | 低至中 | ±2 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 不適用 |
| LOC | DAG：200。調整後：約 418（200 × 2.089） | 已觀察／推導 | 低至中 | 分佈很寬 |
| 工期 | 約 22 分鐘（與 `foundation-08` 相同的較弱 S 複雜度參照類別） | 推導（較弱） | 低 | ±10 分鐘 |
| Token 使用量 | 未知 | 未知 | 0.0 | 不適用 |
| 複雜度 | S（DAG） | 已觀察 | 中 — `qa` 是一個全新的角色，在本資料集中尚未執行過任何節點；不像本表中其他每個節點都至少有一個同角色的先前節點可供對照，`qa` 沒有專屬的過往紀錄可用來驗證 S 這個標籤 | 純粹因為這會是該角色史上第一個被執行的節點，其不確定性確實比單看 S 標籤所顯示的更高 |
| 執行風險 | 低 — DAG 原文 | 已觀察 | 低至中 — 「新角色的第一個節點」這項風險（見複雜度列）並未被 DAG 逐節點的風險欄位所涵蓋，該欄位只評估節點自身的內容 | 依 `Wave2_Lessons.md` §1 issue #1 的 Windows 錯誤模式，跨平台 CI（3 種作業系統）確實可能揭露真實問題 — 此節點正是設計來捕捉這類問題的機制，因此對「低」這個標籤宜保持些許懷疑 |

### qa-08（治理文件）

| 指標 | 數值 | 來源 | 信心水準 | 不確定性 |
|---|---|---|---|---|
| 變更檔案數 | DAG：4。調整後：約 6（4 × 1.544） | 已觀察／推導 | 低至中 | ±2 |
| 讀取檔案數 | 未知 | 未知 | 0.0 | 不適用 |
| LOC | DAG：300（DAG 本身標記為「(doc)」— 很可能是散文而非程式碼，因此程式碼 LOC 校準比率可能無法完全適用） | 已觀察 | 低 — 這是本組中唯一一個套用 LOC 校準係數存疑的節點，因為該係數完全是由 Go 程式碼＋測試的 LOC 推導而來，並非散文 | 明確標註出來，而非悄悄將程式碼校準比率套用到一個文件節點上 |
| 工期 | 約 22 分鐘（較弱的 S 複雜度參照類別，如上所述有文件 vs. 程式碼的相同保留意見） | 推導（較弱，且可能是錯誤的參照類別） | 低 | 分佈很寬 |
| Token 使用量 | 未知 | 未知 | 0.0 | 不適用 |
| 複雜度 | S（DAG） | 已觀察 | 中 — 與 `qa-01` 相同的「史上第一個 `qa` 節點」保留意見 | 與 `qa-01` 相同 |
| 執行風險 | 低 — DAG 原文：「無 — 可與所有工作平行執行，沒有程式碼相依」 | 已觀察 | 高度信心這確實是低風險 — 這是純文件工作，零程式碼相依，DAG 原文在此處異常明確（不僅僅寫「低」，而是明確寫出「沒有程式碼相依」） | 本表中所有節點裡，風險評估的不確定性最低的一個 |

## 4. 排名（依所要求的四項準則，依序排列）

| 排名 | 節點 | 相依解鎖價值 | 執行風險 | 估計工期（推導） | 合併複雜度 |
|---|---|---|---|---|---|
| 1 | `foundation-06` | **7 個直接下游相依節點**（主導性 — 見 §2） | 高 | 未知（不存在可比對的高風險 M 節點工期參照） | 高（每個後續角色的 migration 都依賴其 schema 決策） |
| 2 | `predictor-05b` | 1 個直接下游相依節點，但引領整條剩餘的 predictor 鏈（`-05c` → `-07` → `-08`+） | 高 | 未知（完全不存在 L 複雜度的工期參照） | 中（自成一個套件，但依其自身風險註記屬於對正確性至關重要） |
| 3 | `runtime-b01` | 1 個直接下游相依節點（`runtime-b09`，與另外 6 個節點共享 — 僅算部分貢獻） | 低（DAG）／低至中（調整後，依根目錄接線協調需求） | 約 30 分鐘（較弱的推導估計） | 中（不能觸碰 `cmd/auspex/main.go`；需要負責人協調根目錄接線） |
| 4 | `qa-08` | 1 個直接下游相依節點（`qa-09`，與另外 6 個節點共享 — 僅算部分貢獻） | 低（高信心標籤） | 約 22 分鐘（較弱的推導估計，有文件 vs. 程式碼的保留意見） | 低（純文件，無程式碼） |
| 5 | `foundation-08` | 0 個直接下游相依節點 | 低 | 約 22 分鐘（較弱的推導估計） | 低（單一角色，界線清楚孤立的優先序測試） |
| 6 | `qa-01` | 0 個直接下游相依節點 | 低（標籤）／低至中（調整後，新角色首個節點的不確定性） | 約 22 分鐘（較弱的推導估計） | 中（CI 工作流程檔案，橫跨 `.github/**` 的工具設定，且必須在 3 種作業系統上實際通過） |

**如何解讀此排名：** 第 1、2 名並非勢均力敵的判斷 — `foundation-06` 解鎖 7 個節點的價值，以及 `predictor-05b` 作為解鎖剩餘 predictor 流程唯一路徑的地位，都在各自那一列全面壓倒其他準則。第 3 到 6 名則確實相當接近（DAG 風險皆為低、工期皆約為 S／M 等級、解鎖價值皆為部分或零）— 它們之間的排序依據的是較弱、較偏定性的訊號（合併複雜度、新角色首個節點的不確定性），而非單一主導因素，一位理性的規劃者完全可以在不違背本報告證據的前提下，為第 3 到 6 名重新排序。

## 5. 本文件不做的事

依明確指示：以上未指派任何隊友、未有任何節點被啟動，撰寫本文件本身也未建立任何分支／commit／合併。這僅是規劃用的輸入資訊，供儲存庫擁有者做出下一步決策之用。
